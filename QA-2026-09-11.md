# 사매 공개 지면 QA — 2026-09-11

> 브랜치 `qa/ui-ux-0911` · 로컬 dev(localhost:3000) · **비로그인 상태**
> 모바일 390×880 / 데스크톱 1440×900 두 폭 전수 + 320~1920 리사이즈 스윕
> 도구: Chrome DevTools Protocol 로 직접 구동(확장 대신) — 스크립트는 세션 스크래치패드

## 범위

**측정함(24개 지면 × 2폭)**
`/` · `/explore` · `/explore/quiz` · `/explore/[mood]` · `/guide` · `/guide/[slug]` ·
`/spots` · `/spots/[slug]` · `/articles` · `/articles/[slug]` · `/photos/[id]` ·
`/photographers/[id]` · `/trust` · `/privacy` · `/terms` · `/terms/ad-consent` ·
`/login` · `/signup` · `/inquiry` · `/event/persona` · `/favorites` · `/notifications` ·
`/my-inquiries` · `/c/[slug]`

**못 함 — 로그인 필요**
고객(예약·채팅·정산) · 작가 스튜디오 13개 지면 · 어드민 30개 지면.
**로그인 폼에 비밀번호를 대신 입력하는 건 안전 규칙상 못 합니다.** 세션을 넘겨받는 방법은 맨 아래.

---

# 🔴 고쳐야 할 것 — 전부 재현됨

## 1. `/c/[slug]` 에서 푸터가 사진 그리드 **위에** 박혀 있다

광고 유입이 전부 `/c/<slug>` 로 들어오는데(`src/proxy.ts`), 그 지면 첫 화면 바로 아래에
사업자 정보·약관 링크 푸터가 있다. 배너 → "커플·우정 추천 보는 중" → **푸터** → 그 다음에 사진.

```
측정   /          푸터 top 7099px / 문서 7483px → 맨 아래         ✅
      /c/couple  푸터 top  422px / 문서 6912px → 아래에 사진 48장 ❌
```

원인은 명확하다. `src/app/(user)/c/[slug]/page.tsx:131` 주석이 그대로 남아 있다 —
*"여기도 무한 스크롤이라 푸터에 못 닿는다 — 피드 시작 전에 안내 링크(홈과 같은 이유)"*.
**홈은 `feat/reachable-footer` 로 "3회 컷 + [사진 더 보기] + 푸터 복원"을 받았는데
카테고리 지면만 옛 방식에 남았다.** 같은 처리를 `/c/[slug]` 에도 옮기면 된다.

## 2. 배너 무한 루프 — **두 바퀴째부터 되감기가 보인다**

인계노트 §1의 육안 확인 항목. 결론은 **실패**다.

트랙 transform 을 40초 관찰한 실측:

```
11.0s  -300%  (전환 0.5s)   ← 복제된 첫 장 도착
11.5s     0%  (전환 0s  )   ✅ 1바퀴: 순간 점프, 안 보임
26.5s     0%  (전환 0.5s)   ❌ 2바퀴: 0.5초 동안 왼쪽으로 좍 되감김
41.5s     0%  (전환 0.5s)   ❌ 3바퀴: 동일
```

`src/components/user/BannerCarousel.tsx:75-79`

```ts
useEffect(() => {
  if (!snapping) return;
  const f = requestAnimationFrame(() => setSnapping(false));   // ← 같은 프레임에 풀린다
  return () => cancelAnimationFrame(f);
}, [snapping]);
```

`useEffect` 는 **페인트 전**에 돌기 때문에 그 안에서 건 rAF 가 같은 프레임에 실행되고,
브라우저가 `translateX(0%)` 를 한 번도 그리지 못한 채 `transition` 이 다시 켜진다.
첫 바퀴만 타이밍이 맞아 우연히 통과한다.

→ **이중 rAF** 로 한 프레임 확실히 넘기거나, `transitionend` 로 명시적으로 처리.

## 3. 창을 **넓히면** 카드가 빈칸으로 남는다 (스크롤해야 복구)

인계노트 §1의 나머지 육안 확인 항목. 이것도 **실패**.

```
1440 → 390 으로 줄이기   빈칸 0개              ✅
 390 → 1440 으로 넓히기  빈칸 19개, 6초 뒤에도 그대로  ❌
 그 상태에서 120px 스크롤 → 즉시 0개 복구
```

`src/components/user/ExploreGallery.tsx:891-924` 의 주석은 이미 원인을 적어 놨다 —
*"스크롤 복원·해시 점프·**리사이즈**처럼 위치가 한 번에 튀는 경우마다 생긴다"*.
그런데 정작 붙은 리스너는 `scroll` 뿐이고 `resize` 가 없다.

```ts
window.addEventListener("scroll", onScroll, { passive: true });   // ← resize 가 빠졌다
```

→ `resize` 를 같이 듣거나 그리드에 `ResizeObserver`. 모바일 **화면 회전**에서도 같은 증상이다.

## 4. 비로그인 `/bookings` → 로그인이 아니라 **홈**으로 떨어진다

4회 시도 4회 재현.

| 지면 | 비로그인 진입 결과 |
|---|---|
| `/notifications` `/chat` `/settings` `/apply` `/studio` `/admin` | `/login?next=...` ✅ (180~330ms) |
| **`/bookings`** | **`/` (홈)** ❌ — `next` 도 같이 사라짐 |
| `/favorites` `/my-inquiries` | 튕기지 않고 빈 상태로 열림 |

HTML 안에는 `/login?next=/bookings;307` 페이로드가 들어 있고 서버 로그에도
`GET /login?next=/bookings 200` 이 찍히는데 최종 도착지는 `/` 다.

의심 지점: `/bookings` 만 **인증 확인보다 데이터 조회가 먼저**다.

```ts
// src/app/(user)/bookings/page.tsx:35
const [me, all, convMap] = await Promise.all([ getCurrentUser(), listMyBookings(), getConversationMap() ]);
if (!me) redirect("/login?next=/bookings");     // :40 — 조회가 끝난 뒤에야 확인
```

`/settings` 는 `const me = await getCurrentUser(); if (!me) redirect(...)` 로 **먼저** 막는다.
같은 순서로 맞추는 게 우선 시도.

## 5. 여러 지면에서 콘솔 예외가 항상 뜬다

```
Uncaught TypeError: Failed to execute 'measure' on 'Performance':
  'InquiryPage' cannot have a negative time stamp.
```
`InquiryPage` · `NotificationsPage` · `ChatListPage` · `SettingsPage` · `ApplyPage` 에서 재현.
성능 계측 코드가 음수 타임스탬프를 넘기고 있다. 기능은 안 죽지만 Sentry 노이즈다.

---

# 🟠 접근성 — 수치 확정

> 대비는 **글자가 실제로 놓인 좌표**에서 `elementsFromPoint` 로 뒤에 깔린 요소를 페인트
> 순서대로 알파 합성해 계산했다. 사진·그라디언트 위 글자는 자동 판정에서 제외했다.
> (인계노트가 경고한 오탐 두 종류 — 형제 pill 배경, 이미지 위 글자 — 를 이 방식으로 걷어냈다)

## 6. 브랜드 레드가 **글자색으로 쓰이면** 기준 미달 — 전 지면

`#FF3D2E` on `#F4F5F6` = **3.23:1** (본문 기준 4.5 필요)

로고 "samae"(18~20px) · 섹션 번호 01/02/03 · "따로 보기 →" · "30초 취향 테스트" ·
`bg-brand/10` 배지 글자 전반.

## 7. 흰 글씨를 브랜드 레드 위에 얹어도 미달

`#FFFFFF` on `#FF3D2E` = **3.52:1**

- 하단 내비 **활성 탭 "홈"/"매거진"** (14px w600)
- `/explore/[mood]` 메인 CTA **"무료로 견적 받아보기"** (14px w700)

전환 동선의 제일 중요한 버튼이 여기 걸린다.

**제안 — 면 색과 글자 색 토큰을 분리**

| 용도 | 지금 | 제안 | 대비 |
|---|---|---|---|
| 면(pill·버튼 배경) | `#FF3D2E` | 그대로 | — |
| 그 위 흰 글자 | `#FF3D2E` 배경 | `--brand-ink-bg: #C8281C` | 5.56:1 |
| 글자색 브랜드 | `#FF3D2E` | `--brand-ink: #C8281C` | 5.09:1 |

## 8. `--faint` 도 미달 — 10~13px 라벨 전반

`#8A857D` on `#F4F5F6` = **3.36:1**

푸터 사업자 정보 · 날짜 · "함께 보면 좋은 글" · 지역 태그 · 숫자 카운트 · 화살표 →.
인계노트에 "50건대"로 적힌 그 항목이고, 실제로는 10px 이하만이 아니라 **11~13px 까지** 걸린다.

→ `#716C64` (4.77:1) 로 올리거나, 인계노트 제안대로 **"10px 이하는 `--muted` 이상만"** 을 규칙화.

## 9. 푸터 링크가 전 지면에서 **높이 20px**

`아티클 36×20` · `촬영 장소 51×20` · `Q&A 26×20` · `인스타그램 60×20` · `카카오톡 채널 75×20` ·
로고 `samae 57×20`. 44px 기준의 절반 이하다. 세로 패딩만 주면 끝난다.

## 10. `/explore/[mood]` 필름스트립 — **32×46 썸네일이 300개**

하단 필름스트립 하나하나가 터치 타겟인데 폭이 32px. 옆 장을 누르기 쉽다.
같은 지면 뒤로가기 버튼도 36×36.

## 11. 제목 구조

| 지면 | 문제 |
|---|---|
| `/explore/quiz` | `h1` 이 2개 |
| `/explore/[mood]` | `h1` 이 **0개** |
| `/articles/[slug]` | `h1` → `h3` 건너뜀 |

## ✅ 포커스 링은 문제 없었다 — 오탐 걸러냄

`el.focus()` 로 재면 "40개 중 38개 포커스링 없음"이 나온다. **거짓이다.**
`:focus-visible` 은 JS focus 로 안 켜진다. **실제 Tab 키를 CDP 로 눌러** 다시 재니
7개 지면 전부 **18탭 중 누락 0개**. 정상이다.

---

# 🟡 체감 속도

## 12. 홈 첫 화면에 **800KB대 원본 JPG 가 3장**

```
811KB · 844KB · 852KB   ← 원본
 45KB ·  46KB           ← 썸네일
```

`next.config.ts` 주석은 *"이미지는 전부 업로드 시 생성한 500px 썸네일(~30~80KB)"* 라고
적혀 있지만 실제로는 아니다. 배너는 `BannerCarousel.tsx:32` 에 *"2000px JPG 를 그대로 쓴다"* 고
명시돼 있다.

홈 LCP **2540ms** — 다른 지면(560~1160ms)의 2~4배. (dev 절대값은 참고만, 상대 비교는 유효)

## 13. 모든 이미지가 `cache-control: no-cache`

Supabase Storage 공개 버킷 기본값. 재방문·지면 이동마다 재검증 왕복이 생기고
CDN·브라우저 캐시 이득이 사라진다. 업로드 시 `cacheControl` 지정으로 해결된다.

## 14. `/explore/[mood]` 가 한 화면에 `<img>` 602개

문서 높이는 880px(한 화면)인데 DOM 이미지 602개, 실제 네트워크 요청 188건.
필름스트립이 전체 목록을 다 올린다. 가상화 대상.

---

# 🔵 설계 판단 — 정훈 방향 필요

## D1 헤더 (확인됨)

데스크톱 1440에서 **로고와 프로필 아이콘 사이 1400px 가 통째로 빈다.**
검색창은 그 아래 한 줄을 전폭으로 또 먹는다. 두 줄 합쳐 ~130px.
→ 인계노트 제안(**브랜드 — 검색 — 프로필 한 줄**)이 맞다. 모바일은 지금이 자연스러움.

## D2 바로가기 좌측 몰림 (확인됨)

칩 5개(매거진·장소·가이드·취향·페르소나)가 좌측 570px 에 몰리고 우측 850px 가 빈다.
모바일은 균등 배치라 문제 없다. **데스크톱 전용 이슈.**

## D3 무드로 보기 (확인됨 + 근거 추가)

- 데스크톱: 카드 8장이 여백 없이 꽉 참, 카드 높이 172px
- 모바일: 라벨이 **"이색적인 분…"** 로 잘림
- 대표 이미지가 상단 정렬이라 **인물 얼굴이 잘린다** ("시크", "극적인 분위기")
- "무드 12개 모두 보기"가 전폭 버튼이라 정작 무드 카드보다 시각적으로 무겁다

정훈이 말한 *"대표 이미지 자체가 무드를 표현함에 부족"* — 이미지 교체 전에 **크롭 기준
(얼굴 안 잘리게)** 만 바꿔도 상당 부분 회수된다. 이건 개발이 할 수 있다.

## 그 밖에 데스크톱에서 눈에 걸린 것

**`/spots`** — 1440에서 장소 18개가 **1열**로만 쌓여 문서가 5200px+. 썸네일도 140px 로 작아
장소 사진이 안 산다. 2~3열 그리드가 맞다.

**작가 프로필** — 좌측 사이드바가 230px 로 좁아 소개글이 4줄로 쪼개진다.
**"예약·문의하기" CTA 가 사이드바 안에 있어 스크롤하면 사라진다** (문서 2535px). sticky 필요.

**FloatingNav 가 데스크톱에서도 하단 중앙에 떠 콘텐츠를 가린다** — 인계노트에 적힌 그대로.

## 사진 상세 (전환 핵심 지면)

- 사진 위·아래 진한 그라디언트가 **사진 자체를 탁하게** 만든다
- 아이콘 3개(공유·+·말풍선)에 **라벨이 없다** — `+` 가 담기인지 좋아요인지 모른다
- 37장 캐러셀인데 진행 표시가 **"1 / 37" 숫자뿐** — 넘겨볼 동기가 안 생긴다
- 작가 카드에 **이름이 없다** ("이 촬영을 진행하는 작가"). *작가 올려치기 금지* 원칙과
  맞물린 부분이라 개발이 못 정한다 — 다만 **누구에게 예약하는지 모른 채 결제 동선에
  들어가는 것**이 신뢰에 미치는 영향은 한 번 볼 값어치가 있다

## `/explore/[mood]` — "위로 밀어 더 보기" 힌트가 **피사체 정중앙**을 가린다

사진이 주인공인 서비스인데 안내 칩이 인물 몸통 위에 떠 있다. 아래쪽으로.

## 빈 상태 · 게이트가 네 갈래

| 지면 | 비로그인 동작 | 빈 상태 |
|---|---|---|
| `/favorites` | 그냥 열림 | 문구만, **CTA 버튼 없음**, 700px 공백 |
| `/my-inquiries` | 그냥 열림 | 아이콘+제목+설명 ✅, CTA 없음 |
| `/notifications` `/chat` `/settings` | 로그인으로 | — |
| `/bookings` | **홈으로** (버그, §4) | — |

같은 성격의 개인화 지면인데 넷이 다 다르게 군다. 하나로 정해야 한다.

## 카테고리 컨텍스트 쿠키가 **30일**

`/c/<slug>` 를 한 번 보면 그 뒤 30일 동안 **홈(`/`)에 들어가도 `/c/<slug>` 로 튕긴다**
(`src/proxy.ts:58`). 빠져나가는 길은 `전체 보기 ✕` 하나뿐인데,
배너 아래 작은 회색 칩이라 눈에 잘 안 띈다. 30일은 길다.

## `/explore` 마퀴가 **탭처럼 보인다**

마스트헤드 바로 아래 `ARTICLES ✳ TREND ✳ SPOTS ✳ Q&A` 가 흐르는데,
`aria-hidden` 장식이라 눌러도 아무 일이 없다. 하필 그 페이지 섹션 이름 그대로다.
누르려다 실패하는 사람이 나온다.

---

# ⚠️ 측정 함정 — 인계노트에 추가할 것

기존 5개에 두 개 더.

1. **`:focus-visible` 은 `el.focus()` 로 안 켜진다.** JS 로 포커스 주고 재면 전 지면이
   "포커스 링 없음"으로 나온다. **CDP `Input.dispatchKeyEvent` 로 진짜 Tab 을 눌러야** 한다.
   (이번에 CRITICAL 로 올릴 뻔했다 — 실제로는 누락 0개)

2. **Turbopack 캐시가 깨지면 특정 라우트만 서버 렌더에 실패한다.**
   인계노트 §2의 *"`/c/couple` 에서 그리드 컨테이너가 `opacity:0`"* — 재현했더니
   `The server could not finish this Suspense boundary` + `Next.js package not found` +
   Turbopack FATAL 이었다. **`.next` 를 지우고 재기동하니 전부 정상.**
   → 앱 버그가 아니었다. **이상 증상이 한 라우트에만 몰리면 `.next` 부터 비울 것.**

3. (보강) 대비 계산에서 **조상 체인만 타면 안 된다.** 활성 탭 pill 처럼 배경이 형제 요소면
   `1.09:1` 같은 헛것이 나온다. `elementsFromPoint(글자좌표)` 로 페인트 순서를 따라가야 한다.
   사진·그라디언트가 뒤에 있으면 **자동 판정을 포기하고 눈으로 볼 목록**으로 빼야 한다.

---

# ✅ 통과한 것

- **등장 연출 빈칸** — 잔존 빈칸 없음. 무한스크롤이 새 묶음을 붙이는 순간 최대 7장이
  잠깐 비지만 **100~200ms 안에 채워진다.** 깜빡임만 남았고 인계노트의 원 증상은 해결됨.
  (단 리사이즈 경로는 §3 참고 — 그쪽은 안 채워진다)
- **리사이즈 가로 넘침 / 글자 잘림** — 320·360·390·430·640·768·1024·1280·1440·1920
  전 폭에서 0건. `/explore` 에서 잡힌 "잘림 5건"은 마퀴(의도)와 6px 서브픽셀이라 무시.
- **기본 접근성** — `html[lang]` · `<title>` · 이미지 `alt` · 입력 라벨 · 이름 없는 링크 0건.
- **가로 스크롤** — 전 지면 0px.

---

# 다음

**바로 손댈 수 있는 것 (버그 4개)**
§1 카테고리 푸터 → §3 리사이즈 sweep → §2 배너 스냅 → §4 `/bookings` 게이트
넷 다 원인과 파일:줄까지 특정됐다. 각각 브랜치 하나씩이면 된다.

**정훈 결정이 필요한 것**
D1 헤더 한 줄 · D2/D3 데스크톱 2단(바로가기+무드) · FloatingNav 데스크톱 처리 ·
게이트 정책 하나로 통일 · 카테고리 쿠키 기간 · 작가 이름 노출 여부

**로그인 구역을 마저 보려면** — 셋 중 하나
1. 헤드리스 크롬을 화면에 띄워(headful) 드릴 테니 그 창에서 직접 로그인 → 이어서 자동 순회
2. 브라우저 확장(Claude in Chrome)으로 **이미 로그인된 크롬**을 붙여 주기 (비밀번호 입력 불필요)
3. 작가/어드민/고객 각 역할의 스크린샷을 직접 찍어 주면 그걸로 리뷰
