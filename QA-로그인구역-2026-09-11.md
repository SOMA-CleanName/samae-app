# 사매 로그인 구역 QA — 2026-09-11

> 정훈 계정(회원 · 작가 김재즈 · 어드민 3역)으로 직접 로그인한 세션을 이어받아 측정.
> 49개 지면 × 모바일 390 / 데스크톱 1440.
> **읽기·스크롤·스크린샷만 했다. 버튼은 하나도 누르지 않았다** — 삭제·승인·초기화 근처도 안 갔다.
> 어드민 화면의 실데이터(회원·거래·연락처)는 이 문서에 옮기지 않는다. 레이아웃 문제만 적는다.

## 범위

| 구역 | 지면 |
|---|---|
| 회원 | `/favorites` `/my-inquiries` `/chat` `/notifications` `/settings` `/apply` `/inquiry` `/explore/quiz` `/event/persona` — 9 |
| 작가 | `/studio` + `profile` `portfolio` `highlights` `packages` `about` `availability` `chat` `bookings` `booking` `settlements` `reviews` `guide` — 13 |
| 어드민 | 대시보드 · 문의 · 채팅 · 거래 · 작가 · 스튜디오 · 회원 · 사진 · 카테고리 · 탐색 · 배정 · 아티클 · 배너 · 태그 · 검색 · 알림 · 고객센터 · 봇KB · 계산기 · 휴지통 · 분석 7종 — 27 |

---

# ✅ 고친 것

## 1. 모바일에서 지면이 가로로 넘치던 두 곳

**앱 전체에서 가로 넘침이 있는 지면은 이 둘뿐이었다.** 둘 다 좁은 폭에서 안 접히는 입력 행.

### `/studio/availability` — 83px 넘침

```
grid-cols-[1.3fr_1fr_1fr_auto]   날짜 · 시작 · 종료 · [차단] 을 한 줄에
```

`fr` 로 나눠도 소용없다. `input[type=date]`·`input[type=time]` 은 **네이티브 최소 너비**(~118px)가
있어서 그 아래로는 안 줄어든다. 폰에서는 날짜 / 시작·종료 / 버튼 세 줄로 접었다.

### `/admin/calculator` — 61px 넘침

한 열짜리 그리드의 열이 **422px 로 굳어** 있었다. 그리드 항목은 `min-width:auto` 라
max-content 밑으로 안 줄어든다. 모바일 열을 `minmax(0,1fr)` 로 명시했다(같은 패턴 3곳).
숫자 입력 행도 라벨만 줄어들게(`min-w-0 flex-1`) / 숫자칸은 고정(`shrink-0`).

> 처음엔 상단 탭(`StudioSidebar`·`AdminNav`)이 원인처럼 보였다. 그런데 그 탭들은 모든 하위
> 지면에 있는데 이 둘만 넘쳤다. **탭은 증상이었다** — 문서가 넓어지니 탭 항목도 화면 밖으로
> 나간 것뿐. "가로 스크롤 조상이 없는데 넘친 요소"로 좁혀서 진짜 원인을 찾았다.

## 2. 대비 — 상태 토큰 두 개를 갈랐다

`--*-ink` 가 라이트 모드에서 채움색을 그대로 별칭하고 있었다. 넷 중 둘이 그걸로는 모자랐다.

| 토큰 | 전 | 후 |
|---|---|---|
| `--danger-ink` | `#dc2626` — soft 위 **3.95:1** | `#b91c1c` — **5.30:1** |
| `--success-ink` | `#15803d` — surface-2 위 **4.24:1** | `#166534` — **6.49:1** |
| `--warning-ink` | 4.51 | 그대로 |
| `--info-ink` | 5.49 | 그대로 |

danger 가 특히 낮은 건 soft 틴트가 넷 중 가장 밝기 때문이다(`#fee2e2`).

그리고 `(admin)`·`(photographer)` 안에서 `text-brand`/`text-danger`/`text-success` 가
base color 로 쓰인 **101곳**을 잉크 토큰으로 옮겼다. 공개 지면과 같은 규칙 —
`hover:` 같은 변형은 그대로, **어두운 면 위(`bg-fg`·`text-bg`)는 건드리지 않는다.**

---

# 🟡 보고만 — 판단이 필요한 것

## 3. `/admin/trash` — 지면이 5,200px, 터치 타겟 610개

삭제 묶음 20여 개가 쌓여 있고 **묶음마다 사진 칩을 30개씩 전부 나열**한다.
정작 결정에 필요한 건 "언제 · 누가 · 몇 장"인데, 그건 한 줄이고 나머지가 칩 수백 개다.

→ 칩은 접어 두고(펼치기), 묶음 줄에 요약만 두는 게 맞아 보인다.

## 4. `/studio/availability` — 터치 타겟 173개

시간 슬롯 격자다. 슬롯 하나하나가 작아 손가락으로 정확히 고르기 어렵다.
(가로 넘침은 고쳤지만 슬롯 크기는 설계 문제라 두었다)

## 5. `/admin/categories` 425개 · `/admin/explore` 96개

태그·사진 칩이 잔뜩 깔리는 관리 화면이다. 어드민은 데스크톱에서 쓰는 게 전제라면
지금도 괜찮다. **모바일에서도 쓸 거면** 칩 크기를 올려야 한다 — 어느 쪽인지 정해야 판단이 선다.

## 6. 작가 스튜디오 첫 화면에 **다음 행동이 없다**

데이터가 없는 상태에서 빈 상태 문구가 넷 연속으로 깔린다.

```
지금 처리할 일이 없어요. 새 문의가 오면 여기에 표시됩니다.
수락 대기 0    고객이 보낸 예약 제안이 없어요.
고객 입금 대기 0    입금을 기다리는 예약이 없어요.
촬영 예정 0    예정된 촬영이 없어요.
```

맞는 말이지만, **막 들어온 작가가 뭘 해야 하는지는 어디에도 없다**(포트폴리오 채우기 ·
패키지 등록 · 가능 시간 설정). 화면 아래 600px 가 비어 있다.

## 7. 잔잔한 것

- `/studio/portfolio` 게시물 제목이 `📷게시물` — 이모지와 글자 사이 공백이 없다
- `/studio/portfolio` 액션 셋(편집 / 게시물 비공개 / **게시물 삭제**) 중 삭제만 버튼이 아닌
  빨간 글씨다. 파괴적 동작이 제일 눌리기 쉬운 모양인 건 뒤집혀 있다
- `/admin` 대시보드 카드가 2열인데 항목이 3·3이라 각 그룹 마지막 줄이 반쯤 빈다

---

# ⚪️ 확인했더니 문제가 아니었던 것

| 관측 | 실제 |
|---|---|
| `/chat` → `/studio/chat` 로 튕김 | 작가 계정이라 작가용 채팅으로 보내는 것 — 의도 |
| `/apply` → `/studio` | 이미 작가라 신청 화면이 필요 없다 — 의도 |
| `/studio/bookings` → `/studio` | `/bookings` 와 같은 패턴의 **의도된 차단**(`DisabledStudioBookingsLayout`) |

---

# ⚠️ 측정 도구를 두 번 고쳤다

이번 구역에서 내 스크립트가 **없는 문제를 만들어 냈다.** 둘 다 공개 지면 때는 안 드러났다.

## ① 조상 폴백이 만든 1:1 무더기 (허위 74건)

글자 좌표에서 `elementsFromPoint` 로 배경을 찾는데, 거기 글자 요소가 없으면
**조상을 거슬러 올라가며** 스택에 있는 걸 찾아 썼다. 그러면 거의 항상 `body` 를 집고,
결과가 페이지 배경이 되어 "글자색 = 배경색" 1:1 이 나온다.

- `/admin/categories` 57건 · `/admin/explore` 17건이 전부 헛것이었다
- 원인: 가로 스크롤 탭 안에서 잘린 요소는 그 좌표 위에 없다

→ **못 찾으면 판정을 포기**하도록 바꿨다. 모르면 모른다고 하는 게 낫다.

## ② `oklab()` 을 못 읽어 반투명 층을 통째로 건너뜀

Tailwind v4 는 `bg-bg/90` 같은 알파 변형을 **`oklab(0.96… / 0.9)`** 로 내보낸다.
내 파서는 `rgb()/rgba()` 만 읽어서 그 층을 `null` 로 흘렸고, **뒤에 있는 걸 배경으로 착각**했다.

```
어드민 sticky 헤더(0.9 불투명) 뒤에 검은 버튼 → 헤더를 건너뛰고 검정이 배경으로 잡힘
→ "samae 로고 3.4:1" (실제로는 문제 없음)
```

→ 캔버스에 실제로 칠해 브라우저가 해석한 값을 읽도록 바꿨다. 어떤 색 문법이 와도 된다.

**이 버그는 반투명 배경을 쓰는 모든 지면에 영향을 준다** — 공개 지면 결과도 같은 도구로
다시 돌려 확인했다.

---

# 최종 수치

전 구역(공개 24 + 회원·작가 22 + 어드민 27 = **73지면**) × 두 폭, 고친 도구로 재측정.

| 구역 | 대비 위반 | 가로 넘침 | 제목 구조 |
|---|---|---|---|
| 공개 390 | 0 | 0 | 0 |
| 공개 1440 | 0 | 0 | 0 |
| 회원·작가 390 | 1 | 0 | 0 |
| 회원·작가 1440 | 1 | 0 | 0 |
| 어드민 390 | 10 | 0 | 0 |
| 어드민 1440 | 9 | 0 | 0 |

**가로 넘침 0 · 제목 구조 이상 0 · 대비 위반 22** (`next build` 통과)

### 대비가 어떻게 움직였나

```
도구 고치기 전     0     ← 볼 수 없었던 것(글자색이 oklab 이면 검사에서 빠졌다)
도구 고친 직후   228
text-fg 158곳 정리 후  137
--faint 재조정 · opacity 반영 후   22
```

### 남은 22건 — 전부 어드민 안쪽 배지

| | |
|---|---|
| ~3.0 | 1건 |
| 3.0~3.5 | 5건 |
| 3.5~4.0 | 9건 |
| 4.0~4.5 | 7건 |

`/admin/photos` 의 `낮춤` 배지(warning 채움 위 3.54) · `/admin/explore/assign` 의
`타겟 미지정`(3.11) · `/admin/analytics/photographers` 의 필터 칩(3.00) 같은 것들이다.

**여기서 멈춘다.** 남은 건 **색 채움 위의 배지**라, 글자를 어둡게 하면 배지 의미색이
흐려지고 채움을 어둡게 하면 상태 구분이 약해진다. 토큰 한 번으로 안 풀리고 배지마다
판단이 필요하다. 운영자만 보는 화면이라 우선순위도 낮다 — 지금 상태를 적어 두고 넘긴다.
