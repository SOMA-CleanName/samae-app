# 39. 사진 목적 분류 원칙 (2026-09-14)

> 문서 상태: **대분류·텍스트 우선 자동 분류 적용 완료 · 세부분류 적용 보류**
> 다중 목적: 2026-09-15 구현 및 실제 앱 DB `0116` 반영 완료
>
> 적용 범위: 어드민의 `사진 목적&무드` 작업 화면에서 사용할 내부 전용 사진 목적 태그

## 1. 목표

사진과 포트폴리오를 사용자가 사진을 찍으려는 **목적**에 따라 분류한다.

- 목적 태그는 운영자가 분류와 검색 품질을 관리하기 위한 내부 정보다.
- 일반 사용자 화면을 포함한 다른 화면에는 노출하지 않는다.
- 기본 분류 단위는 포트폴리오이며, 포트폴리오에 지정한 대분류 목록을 소속 사진 전체에 적용한다. 사진 하나에 `웨딩`과 `반려동물`처럼 여러 목적을 함께 지정할 수 있다.
- 포트폴리오 안에 목적이 다른 사진이 섞여 있으면 해당 사진에만 다른 값을 지정할 수 있어야 한다.
- 기존 무드 태그, 생성 태그, 카테고리 및 관련 DB 필드는 수정하거나 재사용하지 않는다. 작가가 직접 입력한 `mood_tags`는 목적 판단용 텍스트로 읽을 수 있지만 값은 절대 변경하지 않는다.

## 2. 확정 대분류

현재 사용할 사진 목적 대분류는 다음 7개다.

1. 개인
2. 커플
3. 우정
4. 웨딩
5. 반려동물
6. 상업/브랜드
7. 행사

`미분류`는 사진 목적이 아니라 **분류 상태**다. 목적을 판단하지 못한 포트폴리오와 사진은 별도의 `기타` 목적에 넣지 않고 미분류 상태로 남겨 수동 검수한다.

## 3. 세부분류 체계

> **2026-09-18 확정·저장 시작 (0133).** 처음엔 "구조만 정의하고 저장하지 않는다" 였다. 검색에서 "임신"·"만삭"·"돌" 이
> 모두 행사 사진 25장을 똑같이 보여줘 — 행사 안에 만삭·돌잔치·졸업이 섞여 있었다 — 세부분류를 저장하기로 했다.

체계는 **`src/lib/purpose-details.json` 한 곳**에 있다. 앱(어드민·검색)과 검색어 분리기(`scripts/embed/query_parse.py`)가
같은 파일을 읽는다. 키를 바꾸면 DB CHECK(`purpose_detail_keys()`, 0133)도 새 마이그레이션으로 바꾼다.

| 대분류 | 세부분류 | 검색에서 이 세부분류로 좁히는 말 (일부) |
|---|---|---|
| 개인 | 개인 스냅 · 프로필 · 바디프로필 · 증명·여권 | 프로필 / 바디프로필·바프 / 증명사진·여권사진·취업사진 |
| 커플 | 커플 스냅 · 기념일·프러포즈 | 기념일·프러포즈·청혼 (신혼여행·허니문은 커플 전체) |
| 우정 | 우정 스냅 · 형제·자매 | 형제·자매·남매 |
| 웨딩 | 본식 · 웨딩촬영 · 리마인드 | 본식·결혼식·하객·브라이덜샤워 / 웨딩촬영·야외웨딩·셀프웨딩 / 리마인드웨딩 |
| 반려동물 | 강아지 · 고양이 · 기타 | 강아지·댕댕이·반려견 / 고양이·냥이·반려묘 |
| 상업/브랜드 | 브랜드·광고 · 쇼핑몰·룩북 · 제품 · 비즈니스 프로필 · 음식·공간 | 브랜드·광고 / 쇼핑몰·룩북 / 제품 / 비즈니스 프로필 / 메뉴 |
| 행사 | 만삭 · 아기 · 돌 · 가족 · 졸업 · 단체·동호회 · 연회 | 만삭·임신·임산부 / 아기·신생아·백일 / 돌·돌잔치 / 가족 / 졸업 / 단체·동호회 / 생일·환갑·칠순 |

**정한 것 (2026-09-18)**

- 웨딩은 **언제 찍나**로 나눈다 — 식 당일(본식) / 식 전 촬영(웨딩촬영) / 결혼 후(리마인드). 옛 원안 "웨딩 사진 / 웨딩 스냅" 은 기준이 없어 버렸다
- 상업은 원안 6개(상업·브랜드·쇼핑몰·비즈니스 프로필·제품·광고)가 겹쳐 5개로 줄였다(상업·브랜드·광고를 하나로, 음식·공간을 새로)
- 개인에 **바디프로필**을 따로 둔다 — 검색이 많고 촬영 성격이 다르다. 성별은 세부분류가 아니라 별도 칸(§3.2)
- 신혼여행 → 커플, 형제·자매 → 우정, 가족 → 행사, "돌" 한 글자 → 행사(돌)
- **커플 "여행·신혼여행" 은 뺐다(0134).** 자동 초안 6개가 전부 사진으로만 정해졌고 검수해 보니 커플 스냅과 가르기 어려웠다.
  들어간 값은 커플 스냅으로 옮겼고, "신혼여행"·"허니문" 검색은 커플 전체를 보여준다
- **스냅류(개인 스냅·커플 스냅·우정 스냅)는 검색 말에 잇지 않는다.** "커플스냅" 은 커플 전체를 찾는 말이다 — 커플 스냅으로만
  좁히면 기념일 사진이 빠진다. 스냅류는 어드민 분류에만 쓴다
- 화보·컨셉·인테리어는 **목적이 아니다.** "어떤 느낌으로" 를 말할 뿐이다 — 목적은 함께 쓴 말이 정하고("쇼핑몰 화보" → 상업),
  혼자 오면 무드로 SigLIP 에 넘긴다

**개인 스냅 / 프로필 가르기** — 실내·실외는 기준이 아니다. **누가 봐도 프로필**일 때만 프로필이고, 애매하면 개인 스냅이다.

| 순서 | 조건 | 결과 | 예 |
|---|---|---|---|
| 1 | 설명에 "프로필" + 사진 절반 이상이 프로필 쪽 | 프로필 | Frame Ryu "개인스냅: 프로필" |
| 2 | 설명에 "프로필" 이어도 사진이 누가 봐도 스냅(컨셉·자세 변화) | 개인 스냅 | 모글 "개인 프로필 - 한지" |
| 3 | 설명에 "스냅" | 개인 스냅 | 규빈 "야외감성스냅" (얼굴 클로즈업이어도) |
| 4 | 사진 90% 이상이 프로필 쪽 + 구도가 거의 반복(사진끼리 유사도 0.93↑) | 프로필 | 소노그래피 (거의 같은 두 장) |
| 5 | 나머지 | 개인 스냅 | 스튜디오라도 자세가 다양하면(주미 태권도·컨셉) 스냅 |

"프로필 쪽" 은 사진마다 뚜렷한 프로필 문장(헤드샷·어깨 위·정면) 셋과 스냅 문장 여섯(풍경 속·전신·걷는 순간·카메라를 안 봄 …) 중
가장 높은 점수끼리 비교한다 — 스냅 문장을 여럿 두어 스냅 쪽을 세게 만든다. 처음엔 "거리 스냅 vs 스튜디오 인물" 두 문장이라
표가 10/20·26/45 처럼 반반이었고, 검수에서 프로필 대부분이 스냅으로 고쳐졌다.

**새 기준은 앞으로 들어오는 포트폴리오에 쓴다(2026-09-18 결정).** 이미 있는 포트폴리오는 운영자가 검수해 확정했고, 기준을
바꿔도 건드리지 않는다. 검수 결과와 새 기준이 다른 곳이 5개 있다 — 도토리("호리존 프로필")·Fadin' Photo("프로필")·주미 스튜디오
컨셉 3개는 **검수에서 프로필**, 새 기준으로는 스냅이다. 새 기준이 "애매하면 스냅" 쪽이라 생기는 차이로, 이런 포트폴리오는
어드민에서 프로필로 고친다.

**규칙** (성별 §3.2 와 같다)

| | |
|---|---|
| 값 | `"목적.세부"` 배열 (예: `event.maternity`) — `albums`·`photos` 의 `admin_purpose_details`, 출처 `admin_purpose_details_source`(auto/manual) |
| 붙는 곳 | **그 목적이 있을 때만.** 목적이 빠지면 트리거가 그 목적의 세부분류만 지운다(다른 목적 것은 남는다) |
| 개수 | 한 목적 안에서 여러 개 가능(돌 + 가족) |
| 단위 | 포트폴리오 단위, 필요하면 사진 한 장만 예외 |
| 비었을 때 | `{}` 가 아니라 **null** — 0116 보호 트리거가 작가의 사진 insert 에서 admin_purpose% 칸이 null 인지 본다 |

함수: `set_album_admin_details` · `set_photo_admin_details`(관리자) · `apply_album_details_draft`(서비스, manual·예외 사진은 건드리지 않음).
예외 해제·새 사진 상속은 세부분류도 복사한다.

**어드민** — 목적 토글 아래, 고른 목적마다 "○○ · 세부" 상자에 세부분류 칩(여러 개). 개인이면 같은 상자에 성별도.
목적 드롭다운의 세부분류, 집계표의 세부분류 줄. 검수는 포트폴리오 통째로 한다 — 성별·세부분류만 따로 보는 상태 필터는 두지 않는다.
자동 초안이 남은 포트폴리오는 검수 버튼이 "초안 확정" 으로 바뀐다.

**자동 초안** — `scripts/draft-purpose-details.cjs`. 목적마다 ① 제목·설명을 검색어 분리기에 넣어 나온 세부분류(글 먼저, §6 과 같은 원칙),
② 없으면 세부분류마다 둔 영어 문장과 사진을 비교해 포트폴리오 안 다수결. 2026-09-18 적용: 포트폴리오 171개·사진 1,799장(auto).
글로 정한 13개(졸업 2, 프로필, 웨딩촬영, 만삭 등)는 믿을 만했고, 사진으로 정한 커플 "여행"·개인 "프로필" 은 검수에서 많이 틀려
여행은 빼고(0134) 프로필 기준을 바꿨다(위). **같은 날 운영자 검수를 마치고 성별·세부분류를 전부 확정(manual)했다** —
남은 자동 초안 0. 초안 스크립트는 manual 을 건드리지 않으므로 다시 돌려도 기존 값은 그대로다.

> 교훈: 검수를 "보고 그대로 둔" 포트폴리오는 auto 로 남아 있었고, 기준을 바꿔 초안을 다시 넣자 그중 7개가 바뀌었다(되돌림).
> 검수가 끝나면 바로 확정해 둔다. 초안을 다시 돌리기 전에는 auto 가 남아 있는지 먼저 본다.

**검색** — `임신` → 행사 중 만삭만. 그 세부분류가 붙은 사진이 아직 없으면(검수 전) 행사 전체로 넓힌다 — 사진이 빠지면 안 된다.
적용 뒤 로컬: 임신 6 · 졸업 19 · 본식 6 · 신혼여행 45 · 강아지 18 · 프로필 277 · 돌 25(돌 사진이 아직 없어 행사 전체).

### 3.1 용어 경계

- `단체 사진`, `돌`, `만삭`, `아기`, `졸업`, `연회`, `동호회`는 독립 대분류가 아니라 모두 `행사`의 세부분류로 취급한다.
- 웨딩은 본식 / 웨딩촬영 / 리마인드로 나눈다(위). 옛 `웨딩 사진`·`웨딩 스냅` 구분은 쓰지 않는다.
- `반려동물`은 강아지 / 고양이 / 기타로 나눈다.
- 한 포트폴리오가 같은 목적 안에서 여러 세부분류에 해당하면 모두 고른다(2026-09-18 — 처음엔 대분류만 지정했다).

### 3.2 개인 목적 안의 성별 (2026-09-18)

개인 목적에는 **성별(여성·남성)**을 하나 더 붙인다. 세부분류와 달리 지금 저장하고 쓴다.

**왜** — 검색창에 "여자"·"남자" 를 치면 개인 사진을 성별로 찾는 것이다. 그런데 SigLIP 점수로는 가를 수 없었다.
개인 사진 1,027장 중 약 1,000장이 여성이라 "여자" 는 평균보다 튀는 사진이 없고, 상위 300장으로 자르면
여자 사진 대부분이 잘렸다("여자" 282장). "남자" 106장에는 남자가 약 24장뿐이었다([docs/29 §12.9](29-siglip-text-search.md)).

**규칙**

| | |
|---|---|
| 붙는 곳 | **개인 목적에만.** 커플·웨딩·우정·행사 사진에는 두 성별이 다 있어 성별을 묻지 않는다 |
| 값 | `female`(여성) · `male`(남성) · 비어 있음(미지정) |
| 단위 | 목적과 같다 — **포트폴리오 단위로 정하고, 필요하면 사진 한 장만 예외** |
| 개인을 빼면 | 성별도 함께 지운다(DB 트리거). 목적을 저장하는 모든 길이 이를 지난다 |
| 출처 | `auto`(자동 초안, 검수 대상) · `manual`(사람이 정함). 자동 초안은 `manual` 을 덮어쓰지 않는다 |

**저장** — `albums`·`photos` 의 `admin_purpose_gender`, `admin_purpose_gender_source` (0132). 이름을 `admin_purpose_` 로
시작하게 지어 0116 의 보호 트리거가 그대로 막는다(관리자·서비스만 변경). 함수:

| 함수 | 누가 | 하는 일 |
|---|---|---|
| `set_album_admin_gender(album, gender)` | 관리자 | 포트폴리오와 예외가 아닌 개인 사진에 저장(`manual`). `null` 이면 지운다 |
| `set_photo_admin_gender(photo, gender)` | 관리자 | 사진 한 장 — 예외로 표시한다 |
| `apply_album_gender_draft(album, gender)` | 서비스 | 자동 초안(`auto`). `manual`·예외 사진은 건드리지 않는다 |

사진 예외 해제(`clear_photo_admin_purpose_override`)와 새 사진 상속(`backfill_inherited_photo_purposes`)은 성별도 함께 복사한다.

**어드민 화면** — 목적 토글에서 "개인" 이 켜져 있을 때만 그 아래 `여성 · 남성` 이 나온다. 적용 버튼이 목적과 함께 저장한다.
목적 드롭다운의 개인·여성 / 개인·남성, 집계표 개인 아래 여성·남성 줄. 자동 초안이 남은 포트폴리오는
검수 버튼이 "성별 확정" 으로 바뀐다.

**자동 초안** — `scripts/draft-personal-gender.cjs`. 사진마다 "여자"·"남자" 텍스트 벡터와의 거리를 비교해(경계 0.005)
포트폴리오 안 다수결로 정한다. 기본은 미리보기, `--apply` 로 저장. 2026-09-18 적용: 개인 포트폴리오 111개 →
**여성 107 · 남성 4**(무루필름 1, 베라치노 2, 규빈 1 — 전부 남자 사진만), 사진 1,169장. 동수·섞인 포트폴리오 없음.
여자 포트폴리오 안에서 남자 쪽으로 판정된 사진 1장(Fadin' Photo, 사람이 작게 찍힘)은 검수 때 예외 여부를 본다.
매일 06:00 배치에는 아직 붙이지 않았다 — 새 개인 포트폴리오는 어드민에서 고른다.

**검색** — 저장된 성별로 거른다(`여자` → 개인·여성 사진 전부, 최신순). 성별 칸이 없는 DB 에서는 검색 때 여자·남자
거리를 비교하는 방식(0131)으로 돌아간다.

## 4. 현재 분류 단계

이번 단계에서 수행하는 작업은 대분류 지정까지만이다.

```text
포트폴리오 확인
  → 해당하는 대분류 1개 이상 선택
  → 포트폴리오 내 모든 사진에 일괄 적용
  → 예외 사진이 있으면 사진 단위로 대분류 재지정
```

이번 단계에서 하지 않는 작업:

- 세부분류 자동 판별
- 세부분류 수동 지정
- 세부분류 값 저장
- 무드 분류 또는 무드 태그 변경
- 기존 목적·카테고리·태그 필드 변경
- 일반 사용자 화면에 목적 태그 노출

## 5. 분류 원칙

- **SigLIP 1차 자동 분류에서 확정하는 목적은 반드시 1개만 선택·저장한다. 복수 목적 지정은 운영자의 수동 적용에서만 허용한다.** 텍스트·SigLIP 혼합 자동 분류에도 같은 단일 목적 원칙을 적용한다.
- 자동 분류 근거에 여러 후보와 점수를 남겨도 확정된 목적을 여러 개 저장하는 것은 아니다. 기존 임계값·충돌 기준에 따라 미분류로 보류하는 정책과 수동 검수값 보호는 유지한다.
- 설명이나 사진만으로 대분류를 확신하기 어려우면 임의로 지정하지 않고 미분류 상태로 둔다.
- 포트폴리오의 대분류와 다른 목적이 명백한 사진만 사진 단위 예외로 처리한다.
- 세부분류에 해당하는 단서가 보여도 현재는 대분류까지만 기록한다. 예를 들어 만삭 촬영은 현재 `행사`까지만 지정한다.
- 목적과 무드는 분리한다. 빈티지, 자연스러움, 따뜻함 등의 느낌은 목적 분류에 사용하지 않는다.

## 6. 확정된 자동 분류 방식

### 6.1 입력과 판정 순서

- 유료 외부 모델을 사용하지 않는다.
- 포트폴리오·사진 제목과 설명, 연결된 실제 패키지 이름과 설명, 작가 작성 해시태그, 타깃·탐색 카테고리를 먼저 판정한다.
- 목적을 직접 나타내는 텍스트가 있으면 해당 텍스트 판정을 우선한다.
- 텍스트에 목적 단서가 없을 때 기존 사진 임베딩과 같은 `google/siglip2-so400m-patch16-naflex` 점수를 사용한다.
- 여러 서비스를 나열한 텍스트는 후보 목적을 제한하고, 그 후보 안에서 SigLIP 점수가 높은 목적을 고른다.
- 강한 텍스트 근거끼리 충돌하면 임의로 확정하지 않고 미분류 상태로 남긴다.
- 기존 `photos.embedding`을 재사용하며 1차 목적 분류를 위해 사진을 다시 내려받거나 이미지 임베딩을 다시 만들지 않는다.
- 대분류별 영어 프롬프트 묶음을 SigLIP 텍스트 타워로 임베딩하고 사진 임베딩과 비교한다.
- 세부분류는 저장하지 않는다. 다만 `행사`처럼 범위가 넓은 대분류는 단체·돌·만삭·아기·졸업·연회·동호회 장면 프롬프트를 대분류 판별 근거로만 사용한다.

### 6.2 저장 위치

별도 분류 테이블을 만들지 않고 `albums`와 `photos`에 목적 전용 신규 컬럼을 직접 추가한다. API에서 조회 가능한 것은 허용하며, 일반 사용자 화면의 쿼리와 컴포넌트에서 표시하지 않는 것을 비노출 기준으로 삼는다.

두 테이블의 공통 신규 컬럼:

| 컬럼 | 의미 |
|---|---|
| `admin_purposes` | 확정된 대분류 목록(`text[]`, `0116`). 미분류는 빈 배열 |
| `admin_purpose` | 기존 자동 분류·백필 호환용 첫 번째 대분류. 목록 첫 항목과 동기화하며 미분류는 `null` |
| `admin_purpose_confidence` | 0~1 상대 확신도. 실제 확률로 해석하지 않음 |
| `admin_purpose_source` | `siglip`, `text`, `hybrid`, `manual` 중 하나 |
| `admin_purpose_reviewed` | 운영자가 확인했는지 여부 |
| `admin_purpose_version` | 모델·프롬프트·집계 규칙 버전 |
| `admin_purpose_at` | 자동 또는 수동 분류 처리 시각 |
| `admin_purpose_evidence` | 사용한 텍스트 구절, 후보 목적, SigLIP 후보 등 운영 검수 근거 |

`photos`에만 추가하는 컬럼:

| 컬럼 | 의미 |
|---|---|
| `admin_purpose_overridden` | `true`면 포트폴리오 일괄값 대신 사진에 지정한 예외값 유지 |
| `title` | 사진별 선택 제목 |
| `caption` | 사진별 선택 설명·캡션 |

`0117`부터 패키지 지정은 작가 선택과 관리자 내부 지정을 분리한다. 같은 작가에게 등록된 패키지만 연결할 수 있다.

| 저장 위치 | 용도 | 표시 및 우선순위 |
|---|---|---|
| `albums.package_id`, `albums.price_krw` | 작가가 직접 선택한 상품과 가격 | 작가 화면에 표시하며 항상 우선한다. |
| `album_admin_packages` | 관리자 수동 지정 또는 가격 대조 결과 | 운영자만 조회한다. 작가·고객에게 지정 여부를 표시하지 않는다. |

관리자 지정은 작가 선택값·사진 가격을 변경하지 않는다. 관리자 화면은 `작가 지정`과 `관리자 지정`을 구분하며, 작가가 이미 선택한 상품은 운영자가 변경할 수 없다. 작가가 나중에 패키지를 저장하면 DB 트리거가 내부 지정을 삭제하고 작가 선택을 적용한다. 이후 작가가 선택을 해제해도 삭제된 내부값이 되살아나지 않는다. 패키지를 고르지 않은 상태에서 설명만 저장하는 경우에는 내부 지정을 유지한다.

내부 지정 RPC `set_album_admin_package`는 관리자 권한·같은 작가 상품·작가 선택 여부를 검증한다. 앨범 행 잠금으로 동시 저장 시에도 작가 선택을 보호한다. 내부 테이블은 RLS로 일반 작가의 조회를 차단하며, 관리자 변경은 이 RPC를 통해서만 허용한다.

목적 분류 입력 역시 작가 선택을 우선하고, 작가 선택이 없으면 내부 지정 상품을 참고한다. 내부 상품에 매칭된 텍스트는 작가가 조회할 수 있는 `albums/photos.admin_purpose_evidence`에 저장하지 않는다. SigLIP 1차 분류는 여전히 목적 하나만 고른다.

`0115` 이전 업로드는 패키지 선택 시 상품 ID 대신 `photos.price_krw`에 가격만 저장했다. 검수 화면은 작가에게 등록된 패키지 개수와 포트폴리오의 상품 지정 상태를 구분한다. 관리자 화면의 `상품 미지정`은 작가 선택과 내부 지정이 모두 비어 있다는 뜻이며, 작가가 패키지를 등록하지 않았다는 뜻이 아니다. 선택 사진의 기존 가격과 각 패키지의 가격을 함께 보여준다.

포트폴리오 분류표에는 `상품 지정`과 `상품 미지정` 개수를 나란히 표시하며, 누르면 해당 포트폴리오만 보여준다. 작가 선택 또는 관리자 내부 지정이 있으면 `상품 지정`에 포함한다. 두 집계는 앨범이 있는 포트폴리오 기준이며 단독 사진은 제외한다. 지정·해제 후 개수와 목록도 함께 갱신한다.

기존 자료의 대조 도구는 `scripts/backfill-portfolio-packages.cjs`다. 기본 실행은 조회와 보고서 저장만 수행하고, `--apply`를 붙이면 다음 조건을 모두 만족하는 포트폴리오를 연결한다.

- 작가 선택과 기존 내부 지정이 모두 비어 있고, 모든 소속 사진에 같은 가격이 저장돼 있어야 한다. 비공개 사진도 함께 확인한다.
- 같은 작가에게 등록된 동일 가격 상품이 정확히 하나여야 한다. 비활성 상품도 후보에 포함해 중복 가격을 놓치지 않는다.
- 이미 저장된 포트폴리오 가격이 있다면 사진 가격과 같아야 한다.

조건을 만족하면 `album_admin_packages`에 내부 지정만 저장한다. 가격 누락·불일치, 동일 가격 상품 중복, 일치 상품 없음, 사진 없음은 확인 목록에 남긴다. 다른 작가의 상품이나 목적·이름 유사도는 연결 근거로 사용하지 않는다. 기존 작가·관리자 지정은 보존하며, 앨범·사진의 가격과 목적·검수 결과는 변경하지 않는다.

```bash
node scripts/backfill-portfolio-packages.cjs
node scripts/backfill-portfolio-packages.cjs --apply
```

`.env.local`의 `SUPABASE_DB_POOLER_URL` 또는 `SUPABASE_DB_URL`로 앱과 같은 Supabase 프로젝트에 접속한다. 적용 전 대조 목록과 원래 연결·가격은 `scripts/embed/out/package-links/<실행 시각>-apply/report.json`에 기록하고, 남은 항목과 상품 후보는 같은 폴더의 `needs-review.csv`에 저장한다. 짧은 트랜잭션 안에서 관련 테이블 변경을 잠시 잠그고, 계획한 연결 및 나머지 데이터 보존을 검증한 뒤 커밋한다.

2026-09-15 가격 대조한 119개 포트폴리오를 운영자 요청에 따라 내부 지정으로 이관했다. 이전 실행 보고서와 적용 당시 값·시각이 일치하는 항목만 이동하고, 작가용 상품·앨범 가격 칸은 대조 전 값으로 돌렸다. 사진 가격과 목적·검수 데이터는 보존했다. 공개 포트폴리오 연결은 118개이며, 관리자 화면의 상품 미지정은 55개다. 공개 자료 중 남은 항목은 동일 가격 상품 중복 15개, 일치 상품 없음 12개, 가격 없음 27개, 사진 가격 불일치 1개다.

가격 값이 없거나 유효하지 않으면 `가격 확인 필요`로 표시한다. 서버에서 새 데이터를 받으면 현재 필터·사진 선택을 유지하면서 목록과 가격을 갱신한다.

허용하는 저장 코드는 `personal`, `couple`, `friendship`, `wedding`, `pet`, `commercial`, `event`다. 화면에서는 각각 개인, 커플, 우정, 웨딩, 반려동물, 상업/브랜드, 행사로 표시한다.

`mood_tags`, 타깃 카테고리, 탐색 카테고리는 분류 입력으로 읽을 수 있다. `generated_tags`, `auto_mood_tags`는 입력에서도 제외한다. 이 기능은 기존 태그·카테고리 열을 쓰거나 변경하지 않는다.

### 6.3 포트폴리오와 사진의 관계

- 1차 자동 분류는 포트폴리오 단위로 결정한다.
- 확정된 포트폴리오의 목적 목록 전체를 소속 사진의 `admin_purposes`에도 실제로 일괄 저장한다.
- 포트폴리오 목적을 나중에 수정하면 `admin_purpose_overridden=false`인 사진의 목록을 함께 교체한다.
- `admin_purpose_overridden=true`인 사진은 포트폴리오 목적이 바뀌어도 기존 사진 목적을 유지한다.
- 사진별 예외를 해제하면 포트폴리오의 목적 목록 전체를 다시 상속한다. 단독 사진의 예외를 해제하면 미분류로 돌아간다.
- 이번 1차 분류에서는 사진별 자동 예외를 만들지 않는다. 운영자가 명백히 다른 사진을 확인했을 때만 사진 예외를 지정한다.

### 6.4 적용 순서

1. 전체 공개 포트폴리오를 대상으로 dry-run한다.
2. 목적별 분포와 1·2위 점수 차이가 작은 경계 사례를 검토한다.
3. 검토 결과로 자동 적용 기준을 정한다.
4. 기준 이상 포트폴리오와 소속 사진에 목적값을 저장한다.
5. 기준 미만 포트폴리오는 `admin_purpose=null`로 남겨 수동 검수한다.
6. 자동 저장 결과는 판정 방식에 따라 `admin_purpose_source='text'`, `'hybrid'`, `'siglip'` 중 하나와 `admin_purpose_reviewed=false`로 기록한다.
7. 운영자가 확정하거나 수정하면 `admin_purpose_source='manual'`, `admin_purpose_reviewed=true`로 바꾼다.

분류 배치는 dry-run이 기본이며 명시적인 apply 옵션이 있어야 DB에 쓴다. 재실행할 때는 운영자가 검수한 값과 사진별 예외를 덮어쓰지 않는다.

기본 모드는 위 보수적 기준을 유지한다. 전체 자동 분류가 필요할 때만 `--force-all`을 명시하며, 이 모드는 공개 여부와 임계값·중앙값/다수결 충돌에 관계없이 포트폴리오 중앙값 점수 1위를 저장한다. 이 경우에도 수동 검수값과 사진별 예외는 덮어쓰지 않는다.

상세 설계: [SigLIP 사진 목적 분류 설계](superpowers/specs/2026-09-14-photo-purpose-classification-design.md)

## 7. 1차 적용 결과 (2026-09-14)

- 분류기 버전: `purpose-v2`
- 자동 적용 임계값: 상대 확신도 `0.90`
- 자동 적용 허용 목적: `couple`, `wedding`, `event`
- 수동 검수 전용 목적: `personal`, `friendship`, `pet`, `commercial`
- 판정 대상: 공개 사진 1,780장, 호환 임베딩이 있는 포트폴리오 173개
- 자동 분류: 포트폴리오 9개, 소속 사진 DB 행 119장
- 분류 입력으로 사용한 공개 사진 분포:
  - 커플 1개 포트폴리오 / 10장
  - 웨딩 6개 포트폴리오 / 78장
  - 행사 2개 포트폴리오 / 29장
- 미분류 유지: 처리 대상 포트폴리오 164개
- 중앙값·사진 다수결 충돌: 36개 포트폴리오
- 일괄 저장 검증: 처리 대상 포트폴리오 173개, 소속 사진 1,809장, 앨범-사진 불일치 0건

첫 dry-run 이미지 시트를 전수 검토한 결과, 반려동물이 포함된 웨딩이 `pet`으로 잡히거나 개인 콘셉트 촬영과 상업 룩북이 겹치는 등 사진만으로 실제 촬영 목적을 확정하기 어려운 사례가 확인됐다. 따라서 높은 점수라도 시각적 대상과 촬영 의도가 쉽게 혼동되는 네 목적은 자동 저장하지 않는다. 해당 후보는 어드민에서 운영자가 직접 분류한다.

전체 DB 기준으로는 앨범 177개 중 9개가 분류됐고 168개가 미분류다. 판정 가능한 공개·호환 사진이 없는 4개 앨범은 배치가 건드리지 않았다. 기존 `mood_tags`, `generated_tags`, `auto_mood_tags`, `target_category_id`의 전후 스냅샷은 1,817개 사진과 177개 앨범 전체에서 완전히 동일했다.

검토 산출물은 `/private/tmp/samae-purpose-analysis/purpose-v1/`에 보관한다. 이 경로는 로컬 검증 자료이며 애플리케이션이나 DB의 일부가 아니다.

### 7.1 전사진 자동 분류 적용 (purpose-v3)

운영 결정에 따라 `--force-all` 모드로 사진이 있는 모든 포트폴리오를 자동 분류했다.

- 누락돼 있던 draft 사진 37장에 기존과 같은 SigLIP2 이미지 임베딩을 먼저 생성했다.
- 임베딩 커버리지: 1,817 / 1,817장
- 분류 대상: 사진이 있는 포트폴리오 175개, 사진 1,817장
- 자동 분류: 포트폴리오 175개, 사진 1,817장
- 빈 포트폴리오: 2개. 사진이 없어 목적을 판정하지 않고 `null`로 유지
- 중앙값·사진 다수결 충돌: 37개. 승인된 원칙에 따라 중앙값 점수 1위 적용
- 상대 확신도 0.90 미만: 158개. 전량 분류 모드이므로 1위 목적 적용
- 저장 실패: 0건
- 앨범-사진 목적 불일치: 0건

사진 기준 최종 분포:

| 목적 | 사진 수 |
|---|---:|
| 개인 | 536 |
| 상업/브랜드 | 324 |
| 커플 | 253 |
| 웨딩 | 222 |
| 반려동물 | 214 |
| 행사 | 143 |
| 우정 | 125 |
| 합계 | 1,817 |

전체 적용 전후 `mood_tags`, `generated_tags`, `auto_mood_tags`, `target_category_id` 스냅샷은 완전히 동일했다. 실행 및 검증 산출물은 `/private/tmp/samae-purpose-analysis/purpose-v3/`에 보관한다.

### 7.2 텍스트 우선 재분류 적용 (`purpose-v4-text-first`)

작가가 입력한 텍스트에 촬영 목적이 명시돼 있으면 이를 우선하고, 유용한 목적 텍스트가 없을 때만 SigLIP 결과를 사용하도록 전체 카탈로그를 다시 분류했다.

- 분류 대상: 사진이 있는 포트폴리오 175개, 사진 1,817장
- 분류 완료: 포트폴리오 174개, 사진 1,817장
- 출처별 포트폴리오: 텍스트 164개, 텍스트+SigLIP 9개, SigLIP 2개
- 출처별 사진: 텍스트 1,709장, 텍스트+SigLIP 100장, SigLIP 8장
- 강한 텍스트 충돌로 미분류: 1개 포트폴리오
- 사진이 없어 미분류 유지: 2개 포트폴리오
- 저장 실패: 0건
- 앨범과 개별 예외가 아닌 사진 사이 목적 불일치: 0건

문제가 보고됐던 포트폴리오 `92c88f6f-9403-4eb1-a1db-19811064d474`는 설명의 `커플 스냅`을 근거로 `couple`에 분류됐다. 이 사례에서 SigLIP의 1위 후보인 `friendship`은 검수 근거로만 남고 최종 목적을 덮어쓰지 않는다.

적용 전후 1,817개 사진의 `mood_tags`, `generated_tags`, `auto_mood_tags`, 177개 앨범의 `target_category_id`, 211개 탐색 카테고리 연결은 건수와 전체 해시가 모두 동일했다. 실행 및 검증 산출물은 `/private/tmp/samae-purpose-analysis/purpose-v4/`에 보관한다.

### 7.3 매일 오전 6시 자동화 (`purpose-v5-daily`, 2026-09-15)

맥미니의 기존 임베딩 배치에 `purpose_backfill.py --apply --daily`를 연결했다. 사진 임베딩을 먼저 저장하고, 공개·호환 임베딩 사진이 있는 미검수 포트폴리오 중 신규·구버전·분류 후 수정된 대상을 분류한다. 사진/포트폴리오 수정 시각과 유효 연결 상품의 수정·내부 지정 시각을 분류 시각과 비교한다. 자동 분류는 7개 목적 중 한 개만 선택하며 기존 텍스트 우선·강한 텍스트 충돌 예외를 유지한다.

검수 완료·수동 지정·사진별 예외는 보존한다. 검수한 포트폴리오의 새 공개 사진은 목적이 비어 있을 때 기존 목적 목록을 상속하고 모델로 재판정하지 않는다. 이 상속에 `0118_daily_photo_purpose_inheritance.sql`의 서비스 전용 RPC를 사용한다. 작가 상품 연결을 먼저 사용하고 운영자 내부 지정은 보조 근거로 사용하되, 내부 상품 근거 문구는 작가가 읽을 수 있는 목적 evidence에 기록하지 않는다.

목적 문장 임베딩도 같은 상주 모델의 `/embed-text-backfill`을 사용해 검색에 양보한다. 대상 0개는 정상 종료하고, 사진 단계 일부 실패 시에도 목적 처리를 이어가되 전체 배치는 실패로 기록한다. 변경이 없는 현재 버전은 건너뛴다. 자세한 반영·실행·로그 확인은 [28 문서 §9](28-embedding-batch-automation.md#daily-purpose)를 따른다.

### 7.4 세부분류 · 성별 초안도 매일 (2026-09-23)

9월 18일 초안을 손으로 한 번 돌려 확정한 뒤로 배치에는 목적 분류만 있었다. 그래서 그 뒤에 올라온 포트폴리오는
목적만 자동으로 붙고 세부분류 · 성별이 비어 있었다(발견: 검수 미완료 7개 전부). 맥미니 배치 끝(4단계, 무드 검색 목록 다음)에
`purpose_drafts.py --apply --daily` 를 붙였다 — 두 Node 초안 스크립트와 같은 규칙을 파이썬으로 옮긴 것이다
(맥미니 배치는 venv 만 쓴다). 저장은 `apply_album_details_draft` · `apply_album_gender_draft` 뿐이라
manual · 사진 예외는 그대로다.

- `--daily` 는 **비어 있는 것만** 채운다 — 세부분류가 없는 목적이 있는 포트폴리오, 성별이 없는 개인 포트폴리오.
  이미 들어간 auto 초안은 다시 쓰지 않는다(§3 교훈: 다시 돌렸더니 검수하며 그대로 둔 7개가 바뀌었다)
- 목적이 바뀌어 트리거가 그 목적의 세부분류를 지우면, 다음 날 그 목적만 비어 있으므로 다시 채워진다
- **새 사진:** 검수한 포트폴리오의 새 사진은 2단계 상속 RPC(`backfill_inherited_photo_purposes`)가 목적과 함께
  세부분류 · 성별도 복사한다. 아직 검수 전인 초안 포트폴리오의 새 사진은 2단계가 목적만 붙이므로, 4단계가
  앨범의 초안 값을 **다시 계산하지 않고 그대로** 그 사진에 복사한다(같은 RPC 로)
- 글 단서는 상주 서버 `/search-query` 로 읽는다. 서버에 kiwipiepy 가 없으면(501) 이 단계를 실패로 끝낸다 —
  사진만으로 조용히 채우지 않는다. 맥미니 갱신 시 `pip install -r scripts/embed/requirements.txt` 후 serve 재시작(docs/38)
- **사진 범위:** 보관(archived)을 뺀 모든 사진 — 비공개(draft)도 센다. 0135 와 같은 원칙(노출과 상관없이 붙여 둔다)
- **초안 단계 실패는 알리지 않는다(사람 결정).** 검수 전 보조 값이라 배치를 실패로 보지 않고 디스코드 알림도 보내지 않는다 —
  로그에 `⚠️ 초안 단계 실패` 만 남는다. 다음 날 비어 있는 것을 다시 채우므로 하루 늦어질 뿐이다.
  사진 백필 · 목적 분류 실패는 지금처럼 알린다
- 첫 미리보기(2026-09-23): 세부분류 7개 모두 개인 스냅(글 "스냅"), 성별 7개 모두 여성 — Node 판과 같다

## 8. 향후 결정 사항

- ~~각 세부분류의 포함·제외 기준~~ · ~~웨딩 사진/스냅 구분~~ · ~~반려동물 세부분류~~ — 2026-09-18 §3 에서 정했다
- ~~세부분류·성별 자동 초안을 매일 06:00 배치에 붙일지~~ — 2026-09-23 붙였다(§7.4)
- 수동 검수 결과가 충분히 쌓인 뒤 자동 적용 목적과 임계값을 확대할지 여부

## 9. 여러 목적을 함께 지정하기 (2026-09-15)

`사진 목적`에서 해당 항목을 여러 개 선택한다. 선택한 항목을 다시 누르면 해제된다. 선택만으로 저장하지 않으며, 선택 영역 아래의 **포트폴리오 전체에 적용** 또는 **선택한 사진만 개별 적용**으로 확정한다. 최소 한 개를 선택해야 적용할 수 있다.

`저신뢰` 목록과 개수는 자동 분류 신뢰도가 0.8 미만이며 아직 검수하지 않은 포트폴리오만 포함한다. 포트폴리오 검수를 완료하면 즉시 저신뢰에서 빠지고, 당시 자동 분류 신뢰도 자체는 기록으로 보존한다.

`검수 미완료`는 신뢰도와 분류 여부에 관계없이 아직 포트폴리오 검수를 완료하지 않은 항목을 모은다. 포트폴리오 검수 완료 시 목록과 개수에서 즉시 빠진다. 단독 사진은 해당 사진의 검수 상태를 기준으로 한다.

왼쪽 필터 아래의 `목적별 집계`는 필터와 관계없이 이 화면에 불러온 전체 공개 사진을 기준으로 한다. 포트폴리오 수는 포트폴리오 자체에 지정된 목적을, 사진 수는 개별 사진에 지정된 목적을 각각 센다. 사진별 예외가 포트폴리오의 목적 개수를 바꾸지는 않는다. 단독 사진은 사진 수에만 포함한다. 복수 목적은 각 목적에 한 번씩 포함하고, `전체` 행은 목적 수와 관계없이 각 포트폴리오·사진을 한 번만 센다. 목적 저장·예외 변경·서버 재조회 시 집계도 갱신된다.

예를 들어 강아지를 안고 찍은 웨딩 사진에는 `웨딩`·`반려동물`을 선택하고 사진만 개별 적용한다. 나머지 사진의 분류는 유지된다. 사진 배지에는 저장된 목적을 모두 표시하며 목적 필터는 포트폴리오 또는 소속 사진에 포함된 목적을 찾는다.

저장소의 [0116 마이그레이션](../supabase/migrations/0116_multiple_photo_purposes.sql)을 앱 DB에 먼저 적용한다. 기존 단일 목적과 검수·신뢰도·출처·근거는 보존하고 목적 목록을 채운다. 기존 단일 목적 RPC와 자동 백필도 계속 동작한다. 자동 분류는 수동 확정·검수한 목적 목록과 사진별 예외를 덮어쓰지 않는다.

DB 접속 설정이 있으면 프로젝트의 기존 실행기를 사용한다.

```bash
node scripts/apply-migration.cjs supabase/migrations/0116_multiple_photo_purposes.sql
```

또는 같은 Supabase 프로젝트의 SQL Editor에서 파일 전체를 한 번 실행한다. 적용 전에는 앱이 기존 단일 목적 데이터로 화면을 열고 DB 업데이트 안내를 표시하며, 복수 목적 저장을 비활성화한다. 적용 후 화면을 새로고침한다.

저장 로직은 격리 PostgreSQL 16에서 [SQL 통합 테스트](../supabase/tests/photo-purpose-multiple.sql)로 검증한다. 테스트는 이름이 `samae_purpose_qa`이고 테이블이 없는 별도 DB만 허용하며, 기존 `0113`~`0115`와 `0116`을 실행한 뒤 전체 트랜잭션을 롤백한다. 운영 DB에 테스트 스크립트를 실행하지 않는다.

2026-09-15 실제 앱 DB(PostgreSQL 17.6)에 `0116` 적용을 완료하고 `_migrations`에 기록했다. 포트폴리오 177개와 사진 1,817개의 기존 데이터는 `updated_at`을 제외한 전체 행 해시로 보존을 확인했으며, 기존 단일 목적과 이관한 목록의 불일치는 0건이다. 웨딩·반려동물 복수 저장, 포트폴리오 상속, 사진 예외 보호·해제, 기존 단일 RPC 호환을 같은 트랜잭션에서 검증하고 시험 변경을 롤백한 뒤 마이그레이션을 커밋했다.

목적 변경 가드는 `SECURITY DEFINER`로 바뀌는 함수 소유자 대신 호출 세션의 역할과 관리자 프로필을 확인한다. 실제 DB에서 일반 로그인 사용자의 RPC 호출 거부와 관리자·서비스 역할의 저장 성공을 확인했다. 앱의 실제 데이터 조회에서도 복수 목적 스키마 인식과 포트폴리오 173개 조회가 확인됐다. 적용된 DB에서 `0116`을 다시 실행하지 않는다.
