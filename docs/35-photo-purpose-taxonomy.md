# 35. 사진 목적 분류 원칙 (2026-09-14)

> 문서 상태: **대분류·SigLIP 1차 분류 설계 확정 · 세부분류 적용 보류 · 구현 미착수**
>
> 적용 범위: 어드민의 `사진 목적&무드` 작업 화면에서 사용할 내부 전용 사진 목적 태그

## 1. 목표

사진과 포트폴리오를 사용자가 사진을 찍으려는 **목적**에 따라 분류한다.

- 목적 태그는 운영자가 분류와 검색 품질을 관리하기 위한 내부 정보다.
- 일반 사용자 화면을 포함한 다른 화면에는 노출하지 않는다.
- 기본 분류 단위는 포트폴리오이며, 포트폴리오에 지정한 대분류를 소속 사진 전체에 적용한다.
- 포트폴리오 안에 목적이 다른 사진이 섞여 있으면 해당 사진에만 다른 값을 지정할 수 있어야 한다.
- 기존 무드 태그, 생성 태그, 카테고리 및 관련 DB 필드는 수정하거나 재사용하지 않는다. 구현 시 이 기능 전용 저장 영역을 새로 만든다.

## 2. 확정 대분류

현재 사용할 사진 목적 대분류는 다음 7개다.

1. 개인
2. 커플
3. 우정
4. 웨딩
5. 반려동물
6. 상업/브랜드
7. 행사

`미분류`는 사진 목적이 아니라 **분류 상태**다. 목적을 판단하지 못한 포트폴리오와 사진은 별도의 `기타` 목적에 넣지 않고 미분류 상태로 남겨 수동 검수한다.

## 3. 세부분류 체계

아래 세부분류는 향후 사용할 분류 구조로만 정의한다. **현재 단계에서는 세부분류를 판별하거나 저장하지 않는다.**

| 대분류 | 예정 세부분류 |
|---|---|
| 개인 | 개인 스냅, 프로필, 증명/여권 |
| 커플 | 커플 스냅 |
| 우정 | 우정 스냅 |
| 웨딩 | 웨딩 사진, 웨딩 스냅 |
| 반려동물 | 미정 |
| 상업/브랜드 | 상업, 브랜드, 쇼핑몰, 비즈니스 프로필, 제품, 광고 |
| 행사 | 단체 사진, 돌, 만삭, 아기, 졸업, 연회, 동호회 |

### 3.1 용어 경계

- `단체 사진`, `돌`, `만삭`, `아기`, `졸업`, `연회`, `동호회`는 독립 대분류가 아니라 모두 `행사`의 세부분류로 취급한다.
- `웨딩 사진`과 `웨딩 스냅`의 구체적인 판별 기준은 세부분류 작업을 시작할 때 별도로 정한다.
- `반려동물`의 세부분류는 아직 만들지 않는다.
- 한 사진이 여러 세부분류 후보에 해당하더라도 현재 단계에서는 대분류만 지정한다.

## 4. 현재 분류 단계

이번 단계에서 수행하는 작업은 대분류 지정까지만이다.

```text
포트폴리오 확인
  → 대분류 1개 지정
  → 포트폴리오 내 모든 사진에 일괄 적용
  → 예외 사진이 있으면 사진 단위로 대분류 재지정
```

이번 단계에서 하지 않는 작업:

- 세부분류 자동 판별
- 세부분류 수동 지정
- 세부분류 값 저장
- 무드 분류 또는 무드 태그 변경
- 기존 목적·카테고리·태그 필드 변경
- 일반 사용자 화면에 목적 태그 노출

## 5. 분류 원칙

- 포트폴리오의 대표적인 촬영 목적을 대분류로 지정한다.
- 설명이나 사진만으로 대분류를 확신하기 어려우면 임의로 지정하지 않고 미분류 상태로 둔다.
- 포트폴리오의 대분류와 다른 목적이 명백한 사진만 사진 단위 예외로 처리한다.
- 세부분류에 해당하는 단서가 보여도 현재는 대분류까지만 기록한다. 예를 들어 만삭 촬영은 현재 `행사`까지만 지정한다.
- 목적과 무드는 분리한다. 빈티지, 자연스러움, 따뜻함 등의 느낌은 목적 분류에 사용하지 않는다.

## 6. 확정된 1차 자동 분류 방식

### 6.1 모델

- 유료 외부 모델을 사용하지 않는다.
- 기존 사진 임베딩과 같은 `google/siglip2-so400m-patch16-naflex`만 사용한다.
- 기존 `photos.embedding`을 재사용하며 1차 목적 분류를 위해 사진을 다시 내려받거나 이미지 임베딩을 다시 만들지 않는다.
- 대분류별 영어 프롬프트 묶음을 SigLIP 텍스트 타워로 임베딩하고 사진 임베딩과 비교한다.
- 세부분류는 저장하지 않는다. 다만 `행사`처럼 범위가 넓은 대분류는 단체·돌·만삭·아기·졸업·연회·동호회 장면 프롬프트를 대분류 판별 근거로만 사용한다.

### 6.2 저장 위치

별도 분류 테이블을 만들지 않고 `albums`와 `photos`에 목적 전용 신규 컬럼을 직접 추가한다. API에서 조회 가능한 것은 허용하며, 일반 사용자 화면의 쿼리와 컴포넌트에서 표시하지 않는 것을 비노출 기준으로 삼는다.

두 테이블의 공통 신규 컬럼:

| 컬럼 | 의미 |
|---|---|
| `admin_purpose` | 확정된 대분류 코드. 미분류는 `null` |
| `admin_purpose_confidence` | 0~1 상대 확신도. 실제 확률로 해석하지 않음 |
| `admin_purpose_source` | `siglip` 또는 `manual` |
| `admin_purpose_reviewed` | 운영자가 확인했는지 여부 |
| `admin_purpose_version` | 모델·프롬프트·집계 규칙 버전 |
| `admin_purpose_at` | 자동 또는 수동 분류 처리 시각 |

`photos`에만 추가하는 컬럼:

| 컬럼 | 의미 |
|---|---|
| `admin_purpose_overridden` | `true`면 포트폴리오 일괄값 대신 사진에 지정한 예외값 유지 |

허용하는 저장 코드는 `personal`, `couple`, `friendship`, `wedding`, `pet`, `commercial`, `event`다. 화면에서는 각각 개인, 커플, 우정, 웨딩, 반려동물, 상업/브랜드, 행사로 표시한다.

기존 `mood_tags`, `generated_tags`, `auto_mood_tags`, `target_category_id` 및 관련 필드는 읽기·쓰기 대상으로 사용하지 않는다.

### 6.3 포트폴리오와 사진의 관계

- 1차 자동 분류는 포트폴리오 단위로 결정한다.
- 확정된 포트폴리오 목적을 소속 사진의 `admin_purpose`에도 실제로 일괄 저장한다.
- 포트폴리오 목적을 나중에 수정하면 `admin_purpose_overridden=false`인 사진만 함께 변경한다.
- `admin_purpose_overridden=true`인 사진은 포트폴리오 목적이 바뀌어도 기존 사진 목적을 유지한다.
- 이번 1차 분류에서는 사진별 자동 예외를 만들지 않는다. 운영자가 명백히 다른 사진을 확인했을 때만 사진 예외를 지정한다.

### 6.4 적용 순서

1. 전체 공개 포트폴리오를 대상으로 dry-run한다.
2. 목적별 분포와 1·2위 점수 차이가 작은 경계 사례를 검토한다.
3. 검토 결과로 자동 적용 기준을 정한다.
4. 기준 이상 포트폴리오와 소속 사진에 목적값을 저장한다.
5. 기준 미만 포트폴리오는 `admin_purpose=null`로 남겨 수동 검수한다.
6. 자동 저장 결과는 `admin_purpose_source='siglip'`, `admin_purpose_reviewed=false`로 기록한다.
7. 운영자가 확정하거나 수정하면 `admin_purpose_source='manual'`, `admin_purpose_reviewed=true`로 바꾼다.

분류 배치는 dry-run이 기본이며 명시적인 apply 옵션이 있어야 DB에 쓴다. 재실행할 때는 운영자가 검수한 값과 사진별 예외를 덮어쓰지 않는다.

기본 모드는 위 보수적 기준을 유지한다. 전체 자동 분류가 필요할 때만 `--force-all`을 명시하며, 이 모드는 공개 여부와 임계값·중앙값/다수결 충돌에 관계없이 포트폴리오 중앙값 점수 1위를 저장한다. 이 경우에도 수동 검수값과 사진별 예외는 덮어쓰지 않는다.

상세 설계: [SigLIP 사진 목적 분류 설계](superpowers/specs/2026-09-14-photo-purpose-classification-design.md)

## 7. 1차 적용 결과 (2026-09-14)

- 분류기 버전: `purpose-v2`
- 자동 적용 임계값: 상대 확신도 `0.90`
- 자동 적용 허용 목적: `couple`, `wedding`, `event`
- 수동 검수 전용 목적: `personal`, `friendship`, `pet`, `commercial`
- 판정 대상: 공개 사진 1,780장, 호환 임베딩이 있는 포트폴리오 173개
- 자동 분류: 포트폴리오 9개, 소속 사진 DB 행 119장
- 분류 입력으로 사용한 공개 사진 분포:
  - 커플 1개 포트폴리오 / 10장
  - 웨딩 6개 포트폴리오 / 78장
  - 행사 2개 포트폴리오 / 29장
- 미분류 유지: 처리 대상 포트폴리오 164개
- 중앙값·사진 다수결 충돌: 36개 포트폴리오
- 일괄 저장 검증: 처리 대상 포트폴리오 173개, 소속 사진 1,809장, 앨범-사진 불일치 0건

첫 dry-run 이미지 시트를 전수 검토한 결과, 반려동물이 포함된 웨딩이 `pet`으로 잡히거나 개인 콘셉트 촬영과 상업 룩북이 겹치는 등 사진만으로 실제 촬영 목적을 확정하기 어려운 사례가 확인됐다. 따라서 높은 점수라도 시각적 대상과 촬영 의도가 쉽게 혼동되는 네 목적은 자동 저장하지 않는다. 해당 후보는 어드민에서 운영자가 직접 분류한다.

전체 DB 기준으로는 앨범 177개 중 9개가 분류됐고 168개가 미분류다. 판정 가능한 공개·호환 사진이 없는 4개 앨범은 배치가 건드리지 않았다. 기존 `mood_tags`, `generated_tags`, `auto_mood_tags`, `target_category_id`의 전후 스냅샷은 1,817개 사진과 177개 앨범 전체에서 완전히 동일했다.

검토 산출물은 `/private/tmp/samae-purpose-analysis/purpose-v1/`에 보관한다. 이 경로는 로컬 검증 자료이며 애플리케이션이나 DB의 일부가 아니다.

### 7.1 전사진 자동 분류 적용 (purpose-v3)

운영 결정에 따라 `--force-all` 모드로 사진이 있는 모든 포트폴리오를 자동 분류했다.

- 누락돼 있던 draft 사진 37장에 기존과 같은 SigLIP2 이미지 임베딩을 먼저 생성했다.
- 임베딩 커버리지: 1,817 / 1,817장
- 분류 대상: 사진이 있는 포트폴리오 175개, 사진 1,817장
- 자동 분류: 포트폴리오 175개, 사진 1,817장
- 빈 포트폴리오: 2개. 사진이 없어 목적을 판정하지 않고 `null`로 유지
- 중앙값·사진 다수결 충돌: 37개. 승인된 원칙에 따라 중앙값 점수 1위 적용
- 상대 확신도 0.90 미만: 158개. 전량 분류 모드이므로 1위 목적 적용
- 저장 실패: 0건
- 앨범-사진 목적 불일치: 0건

사진 기준 최종 분포:

| 목적 | 사진 수 |
|---|---:|
| 개인 | 536 |
| 상업/브랜드 | 324 |
| 커플 | 253 |
| 웨딩 | 222 |
| 반려동물 | 214 |
| 행사 | 143 |
| 우정 | 125 |
| 합계 | 1,817 |

전체 적용 전후 `mood_tags`, `generated_tags`, `auto_mood_tags`, `target_category_id` 스냅샷은 완전히 동일했다. 실행 및 검증 산출물은 `/private/tmp/samae-purpose-analysis/purpose-v3/`에 보관한다.

## 8. 향후 결정 사항

세부분류를 실제로 적용하기 전에 다음 사항을 별도로 확정한다.

- 각 세부분류의 포함·제외 기준
- `웨딩 사진`과 `웨딩 스냅`의 구분
- `반려동물` 세부분류 구성
- 하나의 사진에 여러 목적을 허용할지 여부
- 수동 검수 결과가 충분히 쌓인 뒤 자동 적용 목적과 임계값을 확대할지 여부
