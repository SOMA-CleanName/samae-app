# 29. SigLIP2 검색어 기반 사진 검색 (2026-08-25)

> 문서 상태: 로컬 코드·HTTP 통합 검증 완료 · 화면 수동 확인 필요 · 배포 전
>
> 작업 브랜치: `dev2` (2026-09-14 검색 지연 안내 반영)

## 1. 목표

사용자가 홈 검색창에 자연어를 입력하면 SigLIP2가 검색어를 텍스트 임베딩으로 변환하고, 기존 사진 임베딩과 가까운 공개 사진을 보여준다.

기본 결과는 **SigLIP2 텍스트→이미지 유사도순**을 사용한다. 다만 사진 태그·앨범 제목·앨범 설명처럼 사진과 직접 연결된 메타데이터가 검색어 원문에 일치하면 해당 사진을 우선하고, 유사도가 비슷한 후보 안에서는 홈 추천과 같은 앨범·방향 다양성 규칙을 적용한다.

## 2. 확정 범위

- 모델은 현재 사진 임베딩과 같은 `google/siglip2-so400m-patch16-naflex`를 사용한다.
- 검색창은 홈 히어로 바로 아래, 검색 결과 상단, 사진 상세의 유사사진 영역 위에 노출한다.
- 검색어는 `/?q=<검색어>`로 유지해 새로고침·공유·뒤로가기를 지원한다.
- 문자열 점수와 벡터 거리를 수치로 가중합하지 않는다. 직접 일치 사진을 우선하고, 직접 일치가 부족할 때만 SigLIP2 결과로 보충한 뒤 다양성 재정렬을 적용한다.
- 방향 의도가 없는 검색은 무한 스크롤의 모든 48장 구간을 세로 36장·그 외 12장으로 고정한다. 세로·그 외 후보 큐를 독립 순환하므로 세로 후보가 부족하면 전체 고유 후보 소진 전에 세로 사진이 먼저 반복되며, 모든 후보는 결국 노출한다. 최유사 1위는 첫 위치에 보호하고 동일 앨범은 최근 12장 내 재노출을 피한다.
- `가로`, `세로`, `portrait`, `landscape`처럼 방향 의도가 명시된 검색은 75% 세로 규칙을 적용하지 않는다.
- RRF, 패키지명·패키지 설명 검색, 검색 필터 UI, 검색 자동완성은 이번 범위에서 제외한다.
- 검색 로그는 기존 `search_logs` 경로를 유지한다.

## 3. 데이터 흐름

```text
검색창 제출
  → Next.js 서버에서 두 검색을 병렬 실행
    → 사진 태그·숨김 태그·앨범 제목·설명·장소·작가 메타데이터 직접 일치
    → 맥미니 POST /embed-text → SigLIP2 1152차원 벡터 → similar_photos_by_vector
  → 직접 일치 사진의 실제 RPC 거리·앨범 ID를 보존하고 유사도순으로 정렬
  → 48장마다 직접 일치 최대 36장 + 고유사도 벡터 전용 최대 12장을 후보로 구성
  → 한쪽 후보가 부족하면 다른 후보로 채워 고유 후보를 최대 300장 확보
  → 고유 후보 소진 후에는 검색 상단 순위를 건드리지 않고 사이클별 새 순서로 반복 노출
  → 48장 관련도 구간별 직접 일치·벡터 후보 병합과 앨범 분산 → 화면에서 방향별 큐를 독립 순환 → 방향 의도가 없으면 매 48장마다 세로 36장·그 외 12장 고정
  → 홈 메이슨리 그리드에 표시
```

텍스트와 사진은 동일한 SigLIP2 임베딩 공간에 놓인다. 사진 임베딩의 모델·차원과 검색어 임베딩의 모델·차원이 다르면 비교하지 않고 실패 처리한다.

## 4. 구성요소

### 4.1 맥미니 텍스트 임베딩 API

`scripts/embed/serve.py`에 `POST /embed-text`를 추가한다.

요청:

```json
{ "texts": ["푸른 숲속 커플 사진"] }
```

응답:

```json
{
  "vectors": [[0.01, -0.02]],
  "count": 1,
  "infer_ms": 12,
  "model": "google/siglip2-so400m-patch16-naflex"
}
```

- 기존 `PERSONA_SERVICE_TOKEN` 인증을 그대로 사용한다.
- 공백 제거 후 1~120자만 허용한다.
- 한 요청의 검색어는 최대 8개로 제한한다.
- 기존 상주 SigLIP2 모델을 재사용하고 별도 모델을 띄우지 않는다.

### 4.2 앱 서버 클라이언트

`src/lib`의 서버 전용 모듈이 `/embed-text`를 호출한다.

- 브라우저가 맥미니 주소나 서비스 토큰을 직접 알지 못하게 한다.
- 응답 벡터가 1152차원인지 검증한다.
- 연결 실패·타임아웃·잘못된 응답은 검색 실패로 전달하고, 사진 영역에 §7의 지연 안내와 재시도 버튼을 표시한다. 정상 검색 0건과 구분한다.
- 검색어의 원문은 서버 로그에 남기지 않는다. 기존 집계용 `search_logs`만 사용한다.

### 4.3 벡터 검색

기존 `similar_photos_by_vector(halfvec, integer)` RPC를 서버의 service role로 호출한다.

- 최대 300개의 고유 후보를 요청하고 각 사진의 코사인 거리와 앨범 ID를 앱까지 보존한다. 화면에서는 세로·그 외 후보 큐가 각자의 고유 후보를 먼저 소진하고 이후 결정적 셔플로 독립 순환한다.
- 최유사 후보와 직접 일치 후보는 보호하고, 나머지는 앨범·방향 뭉침을 해소하는 범위에서 재배치한다.
- 앱에서 `GalleryPhoto`에 필요한 작가·가격·지역 메타데이터를 사진 ID로 다시 조회한다.
- 비공개 사진, 미승인 작가, `feed_hidden=true` 사진은 최종 결과에서 제외한다.
- 결과가 부족하더라도 이번 검증 버전에서는 무관한 일반 사진을 뒤에 붙이지 않는다.

## 5. DB 변경 여부

첫 버전은 신규 테이블·컬럼·인덱스·함수를 만들지 않는다.

재사용 대상:

- `photos.embedding halfvec(1152)`
- 사진 임베딩 HNSW 인덱스
- `similar_photos_by_vector` (`0079`)
- 기존 `search_logs`

구현 전에 운영 DB에 `similar_photos_by_vector`가 실제 배포되어 있는지만 읽기 전용으로 확인한다. 함수가 없거나 현재 앱의 노출 규칙을 안전하게 만족시키지 못해 DB 수정이 필요해지면 **마이그레이션을 적용하지 않고 사용자에게 먼저 알린다.**

## 6. UI

### 홈

`FeedHero` 아래, 사진 피드 위에 모서리가 작은 사각형 검색창을 배치한다.

- 검색창의 원래 위치가 화면 상단을 지나면 같은 검색창을 상단에 고정한다. 아래로 이동 중인 고정 검색창은 내부 배경과 문구를 완전히 투명하게 만들고 테두리도 옅은 반투명 색으로 낮춘다. 위로 스크롤하거나 마우스를 올리거나 입력 포커스를 받으면 배경색·문구·기본 테두리를 즉시 복구한다.
- 입력창 플레이스홀더는 공개 사진에서 함께 자주 쓰이는 한국어 태그 조합 중 하나를 요청마다 선택한다. 예: `데이트 커플스냅`, `가을 감성`, `빈티지 필름`.
- 입력창 아래의 별도 예시 문구는 표시하지 않는다.
- 돋보기는 기본 브랜드 빨강이고 입력 포커스 시 테마 본문색(다크 모드 흰색·라이트 모드 검정색)으로 바뀐다.
- 마우스 호버·키보드 포커스 시 브랜드 빨강(`#ff3d2e`) 광택의 앞쪽 끝이 검색창의 실제 SVG 외곽선 왼쪽 위 모서리에서 시작한다. 1.5초 동안 테두리를 한 바퀴 완전히 돌아 앞쪽 끝이 다시 왼쪽 위 모서리에 도착한 뒤, 이동 없이 그 자리에서 사라진다. 한 검색창 인스턴스에서는 최초 1회만 재생하며 다른 화면의 새 검색창에서는 다시 재생한다. 모션 최소화 환경에서는 애니메이션을 끈다.
- 검색 제출 시 `/?q=`로 이동한다.

### 검색 결과

- 홈 히어로는 숨기고 결과 상단에 같은 검색창을 표시한다.
- 사진 상세와 동일하게 화면 왼쪽 위에 고정 뒤로가기를 두고, 검색창은 버튼 오른쪽부터 시작한다. 홈·상세와 같은 스크롤 투명화와 호버·포커스·위쪽 스크롤 복구 효과를 적용한다.
- 입력값은 현재 `q`로 유지한다.
- 상위 결과를 48장씩 점진 노출하며, 하단 센티넬에 닿으면 다음 48장을 이어서 보여준다.
- 검색어별로 불러온 결과와 스크롤 위치를 분리된 세션 키에 유지한다. 최대 300개의 고유 후보를 방향별 큐로 나눠 48장씩 노출하고, 각 큐가 고유 후보를 소진하면 독립 순환해 실제 무한 스크롤을 유지한다.
- 결과가 없으면 `다른 장면이나 분위기로 검색해보세요`를 표시한다.
- 검색이 실패하거나 오래 걸리면 사진 영역 중앙에 사진·시계 아이콘, `대기시간이 오래 걸립니다.`, `잠시 후 다시 시도해주세요.`, 브랜드 색상의 `다시 시도` 버튼을 표시한다. 검색창과 현재 검색어는 유지한다.
- 검색어를 지우고 제출하면 `/`로 돌아간다.
- 하단 홈 탭을 다시 눌러도 검색 상태를 버리고 기본 홈 최상단으로 돌아간다.

### 사진 상세

- 상세 페이지 최상단에서 대표 사진 바로 위에 같은 검색창을 표시한다.
- 뒤로가기는 화면 왼쪽 위의 기존 반투명 고정 버튼을 유지한다. 검색창은 같은 상단 행에서 버튼과 겹치지 않도록 왼쪽 여백을 두고 오른쪽부터 배치하며, 화면 상단에 고정된 동안에는 홈과 동일한 투명·사용 의도 노출 규칙을 적용한다.
- 검색 제출 시 홈의 `/?q=` 검색 결과로 이동한다.

## 7. 장애 처리

- 맥미니 연결 실패, HTTP 오류, 모델·차원 불일치, 검색 DB 오류는 사진 영역의 지연 안내로 처리한다. 부분 검색 결과를 성공으로 표시하거나 장애를 정상 0건으로 기록하지 않는다.
- 모델 큐 대기는 최대 3초, 앱의 `/embed-text` 요청은 최대 4초다. 사진 후보·사용자·관심사진 조회를 포함한 서버 검색 결과 준비에는 총 8초 제한을 두고, 실패·시간 초과 시 진행 중인 검색 HTTP/DB 요청을 취소한다. 사용자 조회는 이 제한 이후 결과 표시를 막지 않는다.
- 검색창과 사진 로딩 영역을 먼저 스트리밍한다. 사진 결과의 Suspense 바깥에서 실행되는 브라우저 타이머도 초기화 후 8초가 지나면 지연 안내를 표시한다. 정상 결과가 준비되면 타이머를 해제하며, 이미 안내로 바뀐 뒤에는 늦은 결과로 자동 전환하지 않는다.
- 공개 검색 URL의 사전 세션 갱신은 별도의 최대 3초 제한을 적용한다. 이 시간과 네트워크·브라우저 초기화 시간은 결과 준비 8초에 포함되지 않으므로, 검색 버튼을 누른 순간부터 8초를 보장하는 의미는 아니다. 보호된 페이지·서버 액션의 인증과 DB 접근 권한 검사는 유지한다.
- `다시 시도`는 해당 검색어의 갤러리·스크롤·앵커 세션 캐시를 지우고 같은 `q`로 새 GET 요청을 보낸다. 다른 검색어의 캐시는 유지한다.
- 성공한 검색의 집계 로그는 응답 이후 기록하여 로깅 지연이 사진 표시를 막지 않도록 한다.
- 메타데이터 직접 일치는 `만삭`·`돌`처럼 시각 모델만으로 찾기 어려운 촬영 목적을 보강하는 확정 검색 경로이며, 임의의 광범위 문자열 폴백은 사용하지 않는다.
- 홈 일반 피드와 사진 상세 유사도 추천은 검색 장애의 영향을 받지 않는다.

## 8. 검증

- `encode_text()`가 1152차원 정규화 벡터를 반환하는지 확인한다.
- `/embed-text` 인증·입력 제한·응답 형식을 테스트한다.
- 앱 클라이언트의 정상 응답·타임아웃·차원 불일치를 테스트한다.
- 정상 0건과 실패를 구분하고, 무응답 시 기한·취소, 늦은 응답, 재시도 시 검색어와 캐시 처리를 테스트한다.
- 벡터 RPC 결과가 공개 사진·승인 작가·`feed_hidden=false`만 노출하는지 확인한다.
- `웨딩`, `푸른 숲속 커플`, `어두운 실내 플래시 인물`, `바다에서 뛰는 사람` 등 대표 검색어로 결과를 눈으로 비교한다.
- 홈과 결과 화면의 모바일·데스크톱 배치, 뒤로가기, 검색어 유지, 빈 결과를 확인한다.

## 9. 배포 순서

맥미니의 상주 서비스 등록·재시작, Funnel, 앱 환경변수, 인증·검색 점검 명령은 [28 임베딩 배치 자동화 §8](28-embedding-batch-automation.md#search-server)을 따른다.

1. 맥미니 런타임에 `/embed-text`를 먼저 반영한다.
2. 인증된 요청으로 모델명·벡터 차원·지연시간을 확인한다.
3. 운영 DB의 기존 `0079` RPC 존재 여부를 읽기 전용으로 확인한다.
4. 앱 검색 UI와 서버 검색 경로를 배포한다.
5. 검색 로그와 대표 검색어 결과를 확인한다.

앱 배포 전에 맥미니 API가 준비되어야 메타데이터 직접 일치가 없는 자연어 검색도 정상적으로 사진을 찾을 수 있다.

백필과 검색의 동시 실행 정책은 [28 문서 §8.7](28-embedding-batch-automation.md#search-priority)을 따른다. 백필도 상주 모델을 공유하고, 사진 한 장이 끝날 때마다 대기 중인 검색부터 처리한다. 이 변경은 맥미니 런타임 갱신과 상주 서비스 재시작 후 적용된다.

## 10. 구현·검증 결과 (2026-08-26)

- `dev`에서 `POST /embed-text`와 홈 검색 UI를 구현했다.
- 실제 응답 모델은 `google/siglip2-so400m-patch16-naflex`, 벡터 차원은 1152로 확인했다.
- 운영 DB의 기존 `similar_photos_by_vector` RPC가 정상 응답했다. **DB 마이그레이션은 만들거나 적용하지 않았다.**
- 워밍된 로컬 모델에서 대표 검색어 텍스트 임베딩은 26.2~94.5ms, RPC는 47.6~898.9ms였다.
- `푸른 숲속 커플 사진`, `어두운 실내 플래시 인물`, `바다에서 뛰는 사람` 세 검색어 모두 상위 8개 결과를 반환했다.
- 로컬 `http://localhost:3000/`과 검색 URL이 HTTP 200을 반환했고, 검색 결과 첫 화면에 48개 카드가 서버 렌더링됐다.
- 검색 고유 후보 상한을 300장으로 확대했다. 기존 갤러리 센티넬로 48장씩 노출하고, 300장 소진 뒤에도 센티넬을 유지해 사이클별 새 순서로 계속 이어지게 했다.
- 포트폴리오 메타데이터 직접 일치 결과를 SigLIP2 앞에 병합했다. 다단어 검색은 원문 단어를 모두 포함할 때만 승격해 일부 흔한 단어가 벡터 순서를 덮지 않게 했다.
- 직접 일치 사진은 실제 SigLIP2 거리로 다시 정렬한다. 각 48장 관련도 구간은 직접 일치 최대 36장과 벡터 전용 최대 12장으로 구성해 고유사도 사진을 상단에 유지하면서 다른 포트폴리오를 분산 후보로 사용한다.
- 검색 화면의 메이슨리는 사진 높이뿐 아니라 동일 앨범의 좌우 겹침과 세로 연속을 함께 고려한다. 일반 홈 피드의 기존 컬럼 배치는 변경하지 않는다.
- 단, 열 높이가 반 장 이상 벌어지면 앨범 분산보다 짧은 열을 우선한다. 분산 점수 때문에 한쪽 열이 비는 현상을 방지한다.
- 검색 후보 생성과 홈·상세 유사사진 추천은 공통 앨범 다양성 재정렬기를 사용한다. 서버는 원본 관련도순을 48장씩 나눠 각 구간 안에서 동일 앨범의 12장 간격을 목표로 한다. 화면의 무한 스크롤은 방향별 후보 큐를 별도로 순환해 모든 48장 구간을 세로 36장·그 외 12장으로 고정한다. 이 비율 보장을 위해 세로 후보가 부족하면 세로 사진이 먼저 반복되고 뒤 관련도 구간의 세로 후보가 앞 화면에 포함될 수 있으며, 최고 유사도 1위는 첫 위치에 보호한다.
- `가로 사진`, `세로형 프로필`, `horizontal` 등 방향 의도가 있는 검색에서는 세로 75% 규칙을 끈다. `가로수길`처럼 방향이 아닌 단어는 예외로 판정한다.
- 운영 데이터에서 `만삭` 직접 일치 사진 6장이 검색 결과 0~5번에 배치되고, `푸른 숲속 커플 사진`은 기존 SigLIP 상위 8개 순서를 유지하는 것을 확인했다.
- 운영 데이터의 `한복`·`가을` 검색 모두 300장 후보와 48장 단위 점진 노출을 확인했다. 첫 48장의 동일 앨범 좌우 겹침은 기존 배치에서 각각 10회·13회였고 새 검색 메이슨리에서 모두 0회였다.
- Python 검색 API 테스트 4개와 전체 Node 테스트 107개, TypeScript 검사와 프로덕션 빌드가 통과했다. 신규 검색 루프·메이슨리 파일 ESLint에도 오류가 없었다.
- 전체 저장소 ESLint에는 이번 변경과 무관한 기존 오류 21개가 남아 있다.
- 자동 브라우저 연결이 제공되지 않아 스크린샷·클릭 자동화는 실행하지 못했다. 3000 서버를 유지해 실제 화면을 수동 확인할 수 있다.

## 11. 검색 지연 안내 검증 (2026-09-14)

- 승인된 시안의 사진·시계 아이콘, 지연 안내 문구, `다시 시도` 버튼을 사진 영역에 반영했다.
- 전체 TypeScript 테스트 271개, TypeScript 검사, 변경 파일 ESLint, 프로덕션 빌드를 통과했다.
- 별도 로컬 프로덕션 서버의 HTTP 통합 검증으로 정상·HTTP 오류·모델 불일치·임베딩 무응답·DB 무응답·DB 오류·정상 0건·재시도 8경로를 확인했다. 임베딩 무응답은 약 4초, DB 무응답은 약 8초 뒤 안내를 렌더링했고 검색 셸은 먼저 응답했다.
- 실제 3000 개발 서버에서 모델·DB 검색과 홈이 HTTP 200과 사진 링크 48개를 반환했다.
- 브라우저 연결이 제공되지 않아 화면 클릭·스크린샷·브라우저 타이머의 실제 초기화는 검증하지 못했다. 원격 배포 전이다.

## 12. 검색어에서 목적을 떼어낸다 (2026-09-18)

### 12.1 왜

검색어를 통째로 SigLIP 에 넣으면 **눈에 뚜렷한 대상이 분위기를 삼킨다.** "가을 커플스냅" 의 벡터는
"커플스냅" 과 0.942, "가을" 과 0.842 로 가까웠고, 상위 20장이 전부 커플이지만 가을은 거의 없었다 —
"커플스냅" 만 검색한 결과와 거의 같았다.

사진마다 목적(개인·커플·우정·웨딩·반려동물·상업/브랜드·행사)은 이미 붙어 있다
([docs/39](39-photo-purpose-taxonomy.md), `photos.admin_purposes`, 공개 사진 전부). 그래서 **목적은 필터로,
나머지만 SigLIP 으로** 쓴다.

### 12.2 흐름

```
앱 → 맥미니 POST /search-query {"query": "가을 커플스냅"}
       Kiwi 로 형태소를 쪼갬 → 목적 사전과 형태소 단위로 대조
       목적 [couple] · 나머지 "가을" → "가을" 만 SigLIP 벡터로
     ← {purposes, mood_text, vector | null}
앱 → "가을" 과 가까운 300장 → 커플 사진을 위로, 나머지는 아래 "비슷한 무드" 로
```

| 검색어 | 목적 | 나머지 | 결과 |
|---|---|---|---|
| 웨딩 | wedding | — | 웨딩 사진 최신순. SigLIP 을 부르지 않는다 |
| 몽환적인 노을 | — | 몽환적인 노을 | 전체에서 SigLIP 순서 (예전과 같다) |
| 가을 커플스냅 | couple | 가을 | "가을" 순서로 커플 45장이 먼저, 다른 가을 사진 255장이 아래 |
| 강아지랑 산책 | pet | 산책 | 반려동물 11장이 먼저, 그 아래 "산책" 사진 |

**목적어는 SigLIP 문구에서 뺀다.** 필터가 이미 커플만 남겼는데 "커플" 을 또 넣으면 다시 분위기를 삼킨다.
"스냅·사진·촬영" 도 뺀다 — 모든 사진이 사진이다.

### 12.3 목적 찾기 — 사전 + 형태소

목적은 7개로 닫혀 있으니 **모델 없이 사전으로** 찾는다. 사전은 사진 목적 분류가 쓰는 `PURPOSE_PHRASES`
([purpose_text.py](../scripts/embed/purpose_text.py))를 그대로 가져온다 — 둘이 따로 놀면 "커플" 로 분류된 사진을
"커플" 로 검색해도 못 찾는다. 사용자가 치는 말(강아지·고양이·남친·여친·가족)은 검색 쪽에만 보탰다
([query_parse.py](../scripts/embed/query_parse.py) `SEARCH_PHRASES`). 분류 사전에 넣으면 매일 오전 6시 목적
분류 결과까지 바뀌기 때문이다. **가족 사진은 행사로 둔다** (2026-09-18 결정).

사전만으로는 낱말 경계를 모른다 — "아기자기한" 에서 "아기" 를 찾아 행사로 잡았다. 그래서 **Kiwi 로 형태소를
쪼갠 뒤 형태소 단위로** 맞춘다. "아기자기" 는 한 형태소라 "아기" 가 나오지 않고, "남친이랑" 은 "남친 + 이랑(조사)" 으로
떨어져 조사까지 함께 뺀다.

검색어에서는 약한 단서(강도 1)도 목적으로 본다. 작가 글의 "커플" 은 "커플 촬영 가능" 같은 나열일 수 있어
확정하지 않았지만, 검색창에 "커플" 이라고 쳤다면 커플을 찾는 것이다. 웨딩과 커플이 함께 나오면 웨딩이다.

Kiwi 는 서버가 아니라 **라이브러리**다. 맥미니 상주 서버(8077) 안에 같이 올라가 포트도 GPU 도 쓰지 않는다
(불러오기 0.4초, 한 번 쪼개는 데 0.04ms, 메모리 약 0.6GB). 검색 순간에 무거운 LLM 을 부르지 않는다.

### 12.4 갱신 전 맥미니와의 호환

`kiwipiepy` 가 없는 맥미니는 `/search-query` 에 501 을 준다. 앱은 그때 예전처럼 검색어 통째로 `/embed-text` 를
부른다 — 맥미니를 갱신하기 전에 앱을 먼저 배포해도 검색이 깨지지 않는다. 갱신 절차는
[docs/38](38-macmini-search-handoff.md).

### 12.5 함께 바뀐 것

- **태그 검색을 쓰지 않는다.** 예전에는 48장 묶음마다 태그 직접 일치 36장 + SigLIP 12장이었다.
- **화면이 순서를 다시 섞지 않는다.** 세로 비율 맞추기(`expandSearchResultLoop`)가 관련도를 안 보고
  목록 전체에서 세로·가로를 번갈아 뽑아, 아래 묶음 사진을 위로 끌어올렸다. 앨범 흩뜨리기는 두 묶음 **안에서 따로** 한다.
- **결과를 다 보여주면 스크롤이 끝난다.** 예전에는 끝에 닿으면 같은 사진을 다시 섞어 끝없이 되풀이했다.

### 12.6 아래 "비슷한 무드의 사진들이에요" 도 검색어의 목적만 (2026-09-18)

처음에는 아래 칸에 **목적이 다른** 가을 사진(개인·웨딩 …)을 깔았다. 검색어에 목적이 있으면
사람이 찾는 건 그 목적이므로, 아래도 **그 목적 사진만** 보여주기로 바꿨다.

```
"가을 커플스냅"
  위   커플 사진 중 "가을" 과 가장 가까운 것 — 전체 사진에서 가까운 300장 안에 든 커플
  아래 나머지 커플 사진을 "가을" 과 가까운 순으로 — "비슷한 무드의 사진들이에요"
```

**이건 검색어에 목적이 있을 때만이다.** "몽환적인 노을" 처럼 목적이 없으면 아래 칸이 없다.

아래 칸을 채우려면 목적 안에서만 거리순으로 줄 세워야 한다. `similar_photos_by_vector` 는 전체에서
가까운 300장만 주는데 거기 든 커플은 이미 전부 위에 있어서, 나머지 커플을 받아올 길이 없었다.
그래서 `similar_photos_in_purposes`(→ §12.8 의 `0131` 로 대체) 를 새로 만들었다. HNSW 인덱스를 **일부러 타지 않는다** —
인덱스로 뽑고 목적으로 거르면 후보 안에서만 걸러져 사진이 적은 목적(반려동물 18장)은 결과가 모자란다.
목적 안의 사진은 많아야 천 장 남짓이라 전부 재도 26~54ms 였다.

| 검색 | 목적 사진 | 위 | 아래 |
|---|---:|---:|---:|
| 가을 × 커플 | 229 | 45 | 184 |
| 산책 × 반려동물 | 18 | 11 | 7 |
| 노을 × 웨딩 | 251 | 61 | 190 |

함수가 없는 DB 에서는 위쪽만 나오고 아래 칸은 비운다 — 목적이 다른 사진을 대신 채우지 않는다.

"남자·여자·남성·여성" 은 개인으로 본다. 다른 목적과 함께 나오면 개인은 물러난다("여자 커플스냅" 은 커플).
"남자친구" 는 형태소로 "남자 + 친구" 로 쪼개지지만 사전의 "남자친구" 도 똑같이 쪼개 긴 것부터 맞추므로 커플이다.

### 12.8 300장 대신 z 로 자른다 (2026-09-18)

300장은 "비슷한지" 가 아니라 그냥 잘라낸 숫자라, 검색어와 무관한 사진까지 채워졌다. 어디까지가 비슷한지를
점수로 정하려고 검색어 9개로 **사진 1,602장 전부**의 점수를 내고, 순위대로 사진을 보며 관련이 끊기는 자리를 찾았다.

**절대 점수로는 못 자른다.** 검색어마다 사진 전체의 평균 점수가 다르다.

| 검색어 | 관련이 끊기는 순위 | 그 자리 점수 | 1위 대비 | **z** |
|---|---:|---:|---:|---:|
| 바다 | 41 | 0.081 | 64% | **2.2** |
| 한복 | 57 | 0.119 | 68% | **2.0** |
| 벚꽃 | 47 | 0.101 | 65% | **2.2** |
| 흑백 | 19 | 0.119 | 88% | **2.7** |

z 는 **그 검색어 점수가 사진 전체 평균에서 표준편차 몇 배 위인가** 다. 네 검색어 합산, 사람 눈으로 센 관련 비율:

| z | 관련 비율 | 화면 |
|---|---:|---|
| 2.5 이상 | 약 90% | 검색 결과 |
| 2.0 ~ 2.5 | 약 55% | "비슷한 무드의 사진들이에요" |
| 2.0 미만 | 약 15% | 보여주지 않는다 |

평균·편차는 **검색 대상 사진 전체**로 잰다. 목적이 있는 검색도 전체 기준 z 를 쓰고 목적으로만 거른다 — 목적 안에서
따로 재면 커플끼리의 상대 순위가 돼서 가장 덜 가을다운 커플 사진도 z 가 높게 나온다.
`0131 search_photos_by_z` 가 한 번에 계산한다. HNSW 를 쓰지 않고 전부 잰다(1,602장, 수십 ms).
목적만 있는 검색("웨딩")은 SigLIP 을 쓰지 않아 z 가 없으므로 최신순 300장 그대로다.

**무드어는 이 컷으로도 좋아지지 않는다.** 장면어는 1위 z 가 3.8~4.8 인데 가을·노을·몽환적인·빈티지는 2.9~3.3 이다.
1위조차 별로 안 튀어나왔다는 건 SigLIP 이 그 말을 못 알아들었다는 뜻이고, 그때는 순서 자체가 틀려서 어디서 잘라도
소용없다. 컷을 넣으면 결과가 **매우 적어진다** — "가을 커플스냅" 은 위 0장·아래 2장, "강아지랑 산책" 은 0장이다.
무드를 영어 문구로 바꾸는 작업([docs/40](40-mood-vocabulary-collection.md))이 끝나야 나아진다.

### 12.7 알고 있는 한계

- z 기준은 **장면어 네 개로 잰 것**이다. 무드어가 영어로 바뀌어 SigLIP 이 알아듣게 되면 같은 기준이 맞는지 다시 재야 한다.
- 사진이 수만 장이 되면 검색마다 전부 재는 게 무거워진다. 그때는 평균·편차를 미리 계산해 둔다.
- **나머지 글자는 아직 한국어 그대로 SigLIP 에 들어간다.** SigLIP 은 한국어 한 단어를 약하게 읽어서, 커플 사진 안의
  "가을" 순서는 초록 여름 사진이 섞일 만큼 약하다. 무드를 영어 문구로 바꾸는 일은 무드 어휘 정리가 끝난 뒤에 한다
  ([docs/40](40-mood-vocabulary-collection.md)).
