# 23. 취향 테스트 '목적' 축 오배선 (2026-08-08)

> 문서 상태: **원인·영향 실측 완료 · 수정 미착수.** 담당자 지정 전
>
> ⚠️ **이 문서는 임시다. 고치고 나면 지운다.** 특정 버그 하나를 팀에 전달하려고 만든 것이라 설계 문서(22 등)와 성격이 다르다. 삭제 조건은 §11.
>
> 한 줄 요약: **"커플 스냅" 으로 판정된 사용자에게 웨딩 사진이 뜬다.** 목적 축이 촬영 종류를 타겟 멤버십이 아니라 탐색 카테고리 슬러그로 판단하는데, 그 슬러그 절반이 **나중에 삭제돼** 실재하지 않기 때문이다(오타가 아니라 드리프트 — §3.1). **취향 테스트·페르소나 이벤트·어드민 대표사진 세 동선이 같은 결함을 공유한다**(§6).
>
> 선행 문서: [20 탐색 편집형 큐레이션](20-explore-editorial-curation.md) · [04 데이터베이스 스키마](04-database-schema.md)
>
> 발견 경위: [22 시각 유사도](22-visual-similarity.md) §7.5 의 8단계 측정 중 곁가지로 확인됐다. 시각 유사도와는 무관한 별개 건이라 문서를 나눴다.

---

## 1. 증상

**"커플 스냅" 으로 판정된 사용자에게 웨딩 사진이 뜬다.** 후보 256장 중 142장(55%)이 웨딩 앨범 사진이다.

- **취향 테스트** → 홈 피드가 그 256장으로 채워진다
- **페르소나 이벤트** → 결과 화면의 "어울리는 사진" 9장이 그 안에서 뽑힌다 (더 직접적이다)

화면이 비거나 에러가 나지 않는다. 피드는 정상적으로 꽉 차므로 **겉으로는 아무 문제가 없어 보인다.** 다만 내용이 취향과 어긋난다.

**웨딩·개인 목적은 정상이다** (86% · 98%). 세 목적 중 하나만의 문제다. 영향받는 동선 전체는 §6.

---

## 2. 먼저 — 카테고리 탭은 멀쩡하다

"커플" 이 두 군데 있어 혼동하기 쉽다. **사용자가 카테고리에서 커플을 눌렀을 때 웨딩이 뜨는 문제는 없다.**

| 경로 | 무엇 | 사진 출처 | 상태 |
|---|---|---|---|
| `/c/couple` "커플·우정" | **타겟** 카테고리 | `resolveTargetPhotoIds` — 앨범 상속 | ✅ 정상 (170장, 전부 커플 앨범) |
| `/explore/date` "데이트" | **탐색** 카테고리 | `explore_category_photos` 수동 배정 | 256장 중 142장이 웨딩 앨범 |

`/c/<slug>` 는 PR #267 이 이미 타겟 멤버십으로 재배선했다. 이번 건은 **목적 축이 내부적으로 `/explore/date` 를 커플의 대역으로 쓰는 것**이고, 증상은 그 목적 축을 쓰는 동선(§6)에서만 나타난다.

---

## 3. 원인 — 카테고리 체계가 둘인데 잘못된 쪽을 본다

| 체계 | 테이블 | 내용 | 멤버십 결정 방식 |
|---|---|---|---|
| **타겟** (촬영 종류) | `categories` (4행) | wedding · couple · snap · etc | **앨범 상속** ∪ 수동추가 − 제외 |
| **탐색** (장면·무드) | `explore_categories` (26행) | `kind='mood'` 5 + `other` 21 | `explore_category_photos` 수동 배정 |

`taste-purposes.ts` 의 목적 3개(웨딩·커플·개인)는 **타겟 축과 같은 개념**이다. 그런데 구현은 `resolveCategoryIdsBySlugs()` 로 **탐색 카테고리** 슬러그를 찾는다.

```
사용자: "커플 스냅" 선택
   ↓
taste-purposes.ts   categorySlugs = ["couple", "date", "friends"]
   ↓                 couple · friends 는 explore_categories 에 없음 → 조용히 버려짐
   ↓                 date 하나만 남음
/explore/date 의 256장  →  그중 142장이 웨딩 앨범 사진
   ↓
fetchTasteHeadIds() → 홈 피드 상위 200장을 이것으로 채움
```

### 3.1 왜 이렇게 됐나 — 오타가 아니라 **설정 드리프트**다

처음엔 두 체계의 슬러그를 혼동한 것으로 봤으나, 마이그레이션 이력을 보면 **작성 당시에는 맞는 슬러그였다.**

```
07-16  0060·0061 이 탐색 카테고리를 시드
       → couple · friends · wedding · self-wedding · profile · hanbok · graduation … 생성됨
07-17  taste-purposes.ts 작성
       커밋: "refactor: 취향 v2 — 목적을 탐색 카테고리 대신 코드 고정 목록(3개)으로"
       → 이 시점에는 위 슬러그가 전부 실재했다
 이후  취향 v2 개편(0066 kind 분류)·어드민 정리로 카테고리 재편
       → 0060 이 넣은 22개가 **전부 사라짐**. 0061 의 25개 중 21개만 남음
```

`0060` 이 시드한 슬러그 22개를 현재 DB 와 대조하면 **하나도 남아 있지 않다.** 그 목록이 §4 의 "존재하지 않는 슬러그" 와 정확히 겹친다. 마이그레이션으로 지운 흔적은 없으므로 어드민에서 수동 정리된 것으로 보인다.

**즉 코드가 틀린 게 아니라, 코드가 참조하는 데이터가 밑에서 바뀌었다.** 이것이 더 나쁜 종류다 — 지금 슬러그를 올바르게 고쳐도 **카테고리를 또 정리하면 다시 썩는다.** 누구도 잘못하지 않았는데 조용히 어긋난다.

이 사실이 §8 의 수정안 선택을 바꾼다. **"경고를 남긴다" 가 곁다리가 아니라 핵심 대응이다.**

`wedding` 과 `couple` 은 **`categories`(타겟) 테이블에는 여전히 존재한다.** 그쪽은 앨범 상속이라 운영자가 카테고리를 정리해도 사라지지 않는다 — B안의 근거이기도 하다.

---

## 4. 실측 (2026-08-08 · 공개 사진 1,763장)

| 목적 | 해석된 슬러그 | 존재하지 않는 슬러그 | 후보 | **후보 중 의도한 촬영 종류** | 그 종류를 담는 비율 |
|---|---|---|---|---|---|
| 개인 스냅 | profile-image · school-uniform | `profile` `graduation` `hanbok` | 576장 | **98%** | 45% |
| 웨딩 스냅 | studio-wedding · outdoor-wedding · casual-wedding · dress | `wedding` `wedding-ceremony` `self-wedding` | 302장 | **86%** | 75% |
| **커플 스냅** | **`date` 하나뿐** | `couple` `friends` | 256장 | **35%** | 53% |

**슬러그가 몇 개 없는지는 문제의 크기가 아니다.** 세 목적 모두 절반 가까이 빠졌지만, 웨딩·개인은 남은 슬러그가 제 역할을 해 86~98% 로 멀쩡하다. 유해한 것은 커플의 `couple`·`friends` 뿐이다.

커플 후보 256장의 실제 촬영 종류 분포:

```
couple  90장 (35%)   ← 의도한 것
wedding 142장 (55%)  ← 문제
snap     24장 (9%)
```

### 4.1 곁가지 — 비공개 카테고리를 쓰고 있다

개인 스냅이 참조하는 `school-uniform`(교복)은 **`published = false`** 다. `resolveCategoryIdsBySlugs` 는 공개 여부를 보지 않기 때문에 그대로 통과한다.

같은 파일의 `listMoodDeckForPurpose`·`listPublishedMoods` 는 `.eq("published", true)` 로 거른다. **동작이 일관되지 않는다.** 사진 자체는 공개된 것들이라 당장 사고는 아니지만, 운영자가 카테고리를 비공개로 내려도 취향 피드에는 계속 반영된다는 뜻이다.

---

## 5. 왜 아무도 몰랐나

`resolveCategoryIdsBySlugs` 가 이렇게 생겼다.

```ts
// src/lib/explore-db.ts
const { data } = await admin
  .from("explore_categories")
  .select("id")
  .in("slug", clean);   // ← 없는 슬러그는 그냥 안 맞히고 끝
```

**`in()` 은 없는 값에 대해 에러도 경고도 로그도 남기지 않는다.** 8개가 소리 없이 사라지고 남은 것으로 피드가 돈다.

게다가 **화면이 비지 않는다.** `fetchTasteHeadIds` 는 `목적∩무드` → `목적만` → `무드만` 순으로 이어붙이는데, 목적 후보가 줄어도 무드 쪽에서 채워져 상위 200장(cap)이 항상 찬다. 목적 3개 × 무드 조합 36가지를 전부 시뮬레이션해도 예외가 없었다.

즉 **"엉뚱한 200장이 채워질 뿐" 이라 발견이 늦는 종류의 결함**이다.

---

## 6. 영향 범위 — 취향 테스트만이 아니다

`resolveCategoryIdsBySlugs` 호출부는 **네 곳**이고, 전부 같은 목적 슬러그를 해석한다. 즉 **세 개의 사용자·운영 동선이 같은 결함을 공유한다.**

| 호출부 | 무엇에 쓰나 | 영향 |
|---|---|---|
| `explore/quiz/actions.ts:29` | **취향 테스트** — 퀴즈 결과를 `samae_taste2` 쿠키로 | ❌ 커플 선택 시 홈 피드 오염 |
| `event/persona/actions.ts:25` | **페르소나 이벤트** — 인스타 분석 결과로 쿠키 세팅 + "어울리는 사진" 9장 | ❌ 같은 문제. `fetchTasteCurated(purposeIds, …)` 로 추천 사진까지 틀어진다 |
| `persona/map-to-taste.ts:19` | 페르소나 → 쿠키 직렬화 (위와 같은 경로) | ❌ 동일 |
| `admin/explore/actions.ts:225` | **어드민** — (무드 × 목적) 대표 사진 후보 고르기 | 🟡 운영자가 **커플 목적의 대표 사진 후보로 웨딩 사진을 보게 된다** |

페르소나 이벤트가 특히 눈에 띈다. 취향 테스트와 달리 **결과 화면에서 "어울리는 사진" 9장을 바로 보여주므로**, 커플로 판정된 사용자에게 웨딩 사진이 즉시 노출된다. 취향 테스트는 홈 피드로 넘어간 뒤라 덜 직접적이다.

어드민 쪽은 사용자에게 안 보이지만, **운영자가 잘못된 후보 중에서 대표 사진을 고르면 그 결과가 `cover_by_purpose` 에 저장돼 취향 테스트 무드 덱에 노출된다.** 오염이 데이터로 굳는 경로다.

### 멀쩡한 것

| | 상태 |
|---|---|
| `/c/couple` 등 카테고리 탭 | ✅ 정상 (타겟 멤버십 — PR #267) |
| `/explore/date` 등 탐색 카테고리 페이지 | ✅ 정상 (의도대로 큐레이션 노출) |
| 취향 테스트·페르소나의 **무드** 축 (5개) | ✅ 정상 — 슬러그가 아니라 카테고리 ID 를 직접 쓴다 |
| **목적** 축 — 웨딩·개인 | 🟡 슬러그 일부 누락이나 결과는 86~98% |
| **목적** 축 — **커플** | ❌ **35%. 위 세 동선 모두 영향** |

**고칠 지점은 한 곳이다.** 네 호출부가 전부 `resolveCategoryIdsBySlugs` + `taste-purposes.ts` 를 거치므로, 그 한 쌍만 바로잡으면 셋 다 동시에 낫는다.

---

## 7. `/explore/date` 자체는 버그가 아닐 수 있다

"데이트" 는 운영자가 직접 고른 큐레이션이다. **장면·분위기로 묶은 모음이므로, 웨딩 앨범 사진이라도 데이트처럼 보이는 컷이면 거기 있는 것이 의도일 수 있다.**

이 문서는 그 컬렉션을 고치라는 것이 아니다. 잘못된 것은 그 장면 컬렉션을 **"커플 스냅이라는 촬영 종류" 의 대역으로 쓴 것**이다.

---

## 8. 고치는 방향

### A. 슬러그만 맞추기 (작음)

`taste-purposes.ts` 의 배열만 수정한다. 커플에 실재하는 슬러그를 넣거나 필요한 탐색 카테고리를 새로 만든다.

- 장점: 변경 범위가 파일 하나
- 단점: **재발 구조가 그대로다.** 실수해서가 아니라 카테고리가 정리될 때마다 저절로 어긋난다(§3.1). 지금 맞춰도 다음 재편에서 또 썩는다
- 현재 쓸 수 있는 비공개 카테고리: `studio` `striking-pose` `school-uniform` `vintage-look` `greenery` `city` `daily-life` (공개 전환 필요)

### B. 타겟 축으로 옮기기 (권장)

목적 = 촬영 종류이므로 `resolveTargetPhotoIds()` 를 쓴다.

- `categories` 4개와 목적 3개가 거의 1:1 대응한다 (wedding / couple / snap)
- **앨범 상속이라 신규 사진이 자동 반영된다.** 현재 앨범 155개 중 149개에 타겟이 지정돼 있다 (snap 102 · wedding 27 · couple 20, 미지정 6)
- PR #267 이 `/c/<slug>` 에 적용한 원칙을 취향 테스트에도 적용하는 것뿐이다
- 단점: `fetchTasteHeadIds` 가 목적·무드 양쪽을 `explore_category_photos` 한 곳에서 읽는 구조라, 목적만 다른 소스로 바꾸려면 그 함수를 손봐야 한다

### 어느 쪽이든 함께 할 것 — 이것이 핵심이다

**없는 슬러그가 조용히 버려지지 않게 한다.** `resolveCategoryIdsBySlugs` 에서 요청 수와 결과 수가 다르면 경고를 남기거나, 개발 환경에서 예외를 던진다.

§3.1 에서 보듯 이 건은 **오타가 아니라 드리프트**다. 슬러그를 지금 올바르게 고쳐도 카테고리를 또 정리하면 다시 어긋난다. **경고가 없으면 그때도 아무도 모른다.** A·B 중 무엇을 택하든 이것을 빼면 같은 일이 반복된다.

`published` 처리도 함께 정한다 (§4.1) — 거를 것인지, 목적 축은 예외로 둘 것인지.

### 우선순위

개인·웨딩이 86~98% 로 멀쩡하므로 전면 개편이 급하지는 않다. **커플 하나만 바로잡아도 사용자가 체감하는 증상은 사라진다.** A 로 급히 막고 B 로 정리하는 순서도 가능하다.

---

## 9. 재현·검증

```bash
node scripts/audit-taste-purposes.cjs
```

읽기 전용이다. 목적별로 슬러그 해석 결과, 타겟 대비 정확도, 없는 슬러그·비공개 카테고리를 출력한다. 슬러그 목록은 `taste-purposes.ts` 에서 직접 읽으므로 **원본을 고치면 이 스크립트 결과도 따라 바뀐다.**

수정 후 **커플 정확도가 60% 이상**으로 올라가는지, 경고가 사라지는지로 확인한다.

2026-08-08 기준 출력:

```
■ 커플 스냅 (key=couple → 타겟 'couple')  ⚠️
   후보 256장 중 실제 couple = 90장  (35%)
   ❌ 존재하지 않는 슬러그 2개: couple, friends
   ❌ 정확도 60% 미만 — 나머지의 실제 타겟: wedding 142 · snap 24

발견된 문제 5건
```

---

## 10. 관련 코드

**고칠 곳 (한 쌍)**

- `src/lib/taste-purposes.ts` — 목적 정의. **슬러그 배열이 여기 있다**
- `src/lib/explore-db.ts` — `resolveCategoryIdsBySlugs` (조용히 버리는 지점)

**영향받는 호출부 4곳** (§6)

- `src/app/(user)/explore/quiz/actions.ts` — 취향 테스트
- `src/app/(user)/event/persona/actions.ts` — 페르소나 이벤트 (결과 사진 9장까지 영향)
- `src/lib/persona/map-to-taste.ts` — 페르소나 → 쿠키 직렬화
- `src/app/(admin)/admin/explore/actions.ts` — 어드민 (무드×목적) 대표 사진 후보

**그 밖에**

- `src/lib/target-categories.ts` — `resolveTargetPhotoIds` (B안이 쓸 것)
- `src/lib/discovery.ts` — `fetchTasteHeadIds` · `fetchHomeFeedPage` (목적·무드를 합쳐 피드 머리를 만드는 곳)
- `src/lib/explore-db.ts` — `listMoodDeckForPurpose` · `listPublishedMoods` (이쪽은 `published` 를 거른다 — §4.1)
- `src/lib/category-constants.ts` — `TASTE_V2_COOKIE` 형식
- `scripts/audit-taste-purposes.cjs` — 감사 스크립트

---

## 11. 같은 조사에서 나온 별건 — `0049` 마이그레이션이 적용된 적이 없다

목적 축과 무관하지만, `_migrations` 추적 격차를 메우다 발견한 건이라 함께 남긴다. **담당은 검색 파트(PR #64, Chelly142)다.**

### 무엇이 없나

`0049_search_logs_and_generated_tag_admin.sql` 이 만들려던 것이 프로덕션에 하나도 없다.

```
search_logs 테이블                : 없음
idx_search_logs_created / _compact : 없음
admin_delete_generated_tag 함수   : 없음
admin_rename_generated_tag 함수   : 없음
```

### 지금 무엇이 깨져 있나

| 코드 | 증상 |
|---|---|
| `admin/tags/actions.ts:19` `admin_delete_generated_tag` | ❌ 어드민에서 **숨김 태그 삭제 시 에러**. `if (error) throw` 라 화면에 그대로 뜬다 |
| `admin/tags/actions.ts:33` `admin_rename_generated_tag` | ❌ **이름변경·병합 시 에러** |
| `lib/search-stats.ts:61` → `/admin/search` | ⚠️ **조용히 빈 화면.** `const { data } = await query` 로 에러를 안 받고 `data ?? []` 로 넘어가, "검색 데이터 없음" 처럼 보인다 |
| `lib/search-log.ts:16` `logSearch()` | 🟡 잠복. 검색 UI 가 없어 호출되지 않는다. **검색을 붙이는 순간 드러난다** |

두 번째 줄이 특히 나쁘다 — 테이블이 없어서 안 나오는 건지 검색이 없어서 안 나오는 건지 화면만 봐선 구분되지 않는다.

### 왜 이것만 빠졌나

**기술적 실패가 아니다.** 파일을 트랜잭션 안에서 실제로 실행해봤고(`rollback` 으로 되돌림, DB 무변경) **문법·권한 문제가 없었다.** 의존하는 `is_admin()`·`gen_random_uuid()`·`profiles` 도 전부 존재한다. 돌리기만 하면 적용된다.

시점을 보면 원인이 보인다.

```
06-22 15:28  0047  fairygina    ← _migrations 추적이 여기서 멈춤
06-24 16:00  0048  kimjazz      ← 적용됨
06-24 18:22  0049  Chelly142    ← ❌ 누락      (PR #64, 19:09 머지)
07-02 17:39  0050  Chelly142    ← 적용됨
07-08 14:49  0051  kimjazz      ← 적용됨
```

추적이 살아 있었다면 `migrate.cjs` 가 "0049 미적용" 을 잡아 실행했을 것이다. **장부가 끊긴 틈에 손으로 적용하다 하나를 건너뛴 것**으로 보인다. `0048`(16:00)과 `0049`(18:22)를 같은 날 다른 사람이 두 시간 차로 올려 더 놓치기 쉬웠다.

즉 **`0049` 누락과 `_migrations` 추적 중단은 같은 사건의 두 얼굴**이다. 원인이 따로 있는 게 아니라, 장부를 안 쓰기 시작하니 하나가 새어나갔다.

### 적용 방법 (담당자 판단)

```bash
node scripts/migrate.cjs 0049_search_logs
```

`migrate.cjs` 접속 문제는 2026-08-08 에 고쳤으므로 정규 경로가 동작한다(docs/22 §11). 파일 전체가 `if not exists` / `create or replace` 라 **재실행해도 안전**하고, `_migrations` 에도 자동 기록된다.

> 이 문서를 쓴 쪽에서 적용하지 않았다. 검색 파트 담당이 판단할 일이고, `search_logs` 는 사용자 검색어를 적재하는 테이블이라 개인정보 취급 판단도 함께 필요해 보인다.

### 참고 — 추적 격차는 메웠다

`_migrations` 를 51개 → **80개**로 백필했다(2026-08-08). 일괄 단언 대신 **파일이 선언한 객체가 DB 에 실제로 있는지 확인한 것만** 기록했고, 그 과정에서 이 건이 드러났다. 남은 미기록 3개는 아래와 같다.

| 파일 | 상태 |
|---|---|
| `0049_search_logs_and_generated_tag_admin.sql` | ❌ **실제로 미적용** — 위 내용 |
| `0061_explore_categories_batch2.sql` | ✅ 적용됨(슬러그 25개 중 21개 존재). 객체를 만들지 않아 스크립트가 자동 판정 못 함 |
| `0069_about_text_section.sql` | ✅ 적용됨(`about_sections_type_check` 에 `'text'` 포함 확인). 제약 변경뿐이라 자동 판정 못 함 |

```bash
node scripts/verify-migrations.cjs           # 점검만
node scripts/verify-migrations.cjs --apply   # 확인된 것만 기록
```

---

## 12. 이 문서의 수명 — 고치면 지운다

**이 문서는 버그 하나를 팀에 전달하려고 만든 임시 문서다.** 22 처럼 설계 결정을 남기는 문서가 아니므로, 역할이 끝나면 `docs/` 에 남겨두지 않는다. 낡은 이슈 문서가 쌓이면 다음 사람이 "이게 아직 유효한 문제인가" 를 매번 확인해야 한다.

**삭제 조건 (전부 충족될 때)**

1. 목적 축 수정이 `main` 에 머지됐다
2. `node scripts/audit-taste-purposes.cjs` 가 **커플 정확도 60% 이상**을 내고, "존재하지 않는 슬러그"·"비공개 카테고리" 경고가 사라졌다
3. **§11 의 `0049` 건이 처리됐다** — 별건이지만 이 문서에 실려 있으므로, 미처리 상태로 문서를 지우면 기록이 함께 사라진다. 담당 파트에서 적용했거나 "적용하지 않는다" 는 판단이 내려진 뒤여야 한다
4. 아래 두 가지가 각각 제자리에 남았다

**지우기 전에 옮길 것**

| 내용 | 옮길 곳 |
|---|---|
| 최종적으로 어떤 구조를 택했는지 (A/B 중 무엇을, 왜) | 커밋 메시지 — 이 repo 는 **커밋이 1차 기록**이다(`CLAUDE.md`). 목적 축이 어느 테이블을 보는지는 나중에 반드시 다시 묻게 된다 |
| 타겟 축 / 탐색 축을 혼동하면 안 된다는 원칙 | [20 탐색 편집형 큐레이션](20-explore-editorial-curation.md) — 두 체계를 설명하는 상설 문서 쪽이 맞다 |
| **설정이 데이터 변경에 조용히 썩는다는 사실** (§3.1) | 위와 같은 곳. 이번 건의 진짜 교훈이라 슬러그 하나 고치는 것으로 끝내면 안 된다 |
| §11 의 `_migrations` 추적·검증 절차 | [22 시각 유사도](22-visual-similarity.md) §12.2 에 이미 있다. 중복이므로 옮길 필요 없음 |

`scripts/audit-taste-purposes.cjs` 는 **남긴다.** 회귀를 잡는 데 계속 쓸 수 있고, 이 문서 없이도 단독으로 읽힌다. 다만 주석의 `docs/23` 참조는 지울 때 함께 정리한다.

[22 시각 유사도](22-visual-similarity.md) §7.7 이 이 문서를 가리키고 있으므로, **삭제 시 그 절도 함께 손본다.** (§7.7 은 "자동 무드 태그보다 이쪽이 취향 피드에 직접적" 이라는 판단 근거로 이 건을 인용한다 — 링크만 빼고 사실은 남길 것)
