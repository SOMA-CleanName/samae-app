# PR 스택 — 9/15 통합

갈래별로 나눈 **9개 PR.** **아래에서 위로 순서대로** 머지해야 한다.

```
qa/photographer-flow-harness  ← feat/tax-and-payment-gate     커밋 12 · 파일 54
feat/tax-and-payment-gate     ← feat/photographer-onboarding  커밋  3 · 파일 24
feat/photographer-onboarding  ← feat/cart-expand-anim         커밋 21 · 파일 47
feat/cart-expand-anim         ← feat/guide-spots-db           커밋  2 · 파일  3
feat/guide-spots-db           ← feat/magazine-deck            커밋  3 · 파일 15
feat/magazine-deck            ← feat/home-first-screen        커밋  4 · 파일  5
feat/home-first-screen        ← qa/ui-ux-0911-stack           커밋 10 · 파일 15
qa/ui-ux-0911-stack           ← chore/sync-dev-main           커밋 51 · 파일 166
chore/sync-dev-main           ← dev                           커밋 20 · 파일 37   ⚠️ 먼저
```

PR 1~6 은 `next build` 통과. eslint 오류는 24개로 **dev 와 동일** — 이 스택이 새로 만든 건 없다.
PR 7~9 도 **`next build` 통과**(2026-09-16: `✓ Compiled successfully`, 정적 페이지 88개) ·
`tsc --noEmit` · `eslint` · 단위 테스트 **337개** 통과.

프로덕션 모드(`next start`)로 `/dev/*` 차단도 실측했다 — `/dev` · `/dev/chat` · `/dev/money` ·
`/dev/refund-test` · `/dev/test-login` · `/dev/pay` 전부 **404**,
`/admin/withholding/export` **403**(라우트 자체 가드), `/` **200**.

### 🔴 마이그레이션이 네 개 붙어 있다 — 머지 순서와 별개로 먼저 적용해야 한다

| 번호 | PR | 내용 | 원격 적용 |
|---|---|---|---|
| 0117 | 7 | `photographer_agreements.doc_records` — 문서별 열람·동의 증적 | ✅ |
| 0118 | 8 | 주민등록번호 3열 + `resident_no_access_logs` | ✅ |
| 0119 | 8 | `bookings.withholding_krw`, `settlement_breakdown` | ✅ |
| 0120 | 9 | 작가 합의 3열 + `bookings.refund_paid_at` | ✅ |

> PR 9 에 **정책 인계 문서(`HANDOFF-POLICY-v1.md`)** 가 들어간다. 그동안 저장소 밖에
> 있어서 «연락처 7일 게이트» 가 미구현인 채로 지나갔다 — 정책 결정이 추적되지 않으면
> 같은 일이 반복된다.

넷 다 **원격에 적용 완료**다. 코드가 먼저 배포되면 없는 컬럼을 조회해 깨지므로 순서를 지킬 것.

---

## PR 1 — `chore/sync-dev-main` → `dev` ⚠️ 먼저 머지

### 왜 지금 해야 하나

**dev 는 #312 이후 main 을 한 번도 안 당겼다.** 그 사이 두 갈래가 각자 쌓였고, 같은 파일에서 만났다.

| | main | dev |
|---|---|---|
| 내용 | 크론 정리(#313) · 카카오 번호(#316·#317) · 알림톡 반려 대응(#318) | 정책 1~7단계 |
| `booking-sweep` | **삭제** | `notifyDeliveryOverdue()` **추가** (5단계) |
| 크론 개수 | 2 | **4** |

이건 내 브랜치가 만든 충돌이 아니다. **dev→main 을 누가 하든 똑같이 밟는다.** 내 작업이 먼저 부딪혔을 뿐이다.

### 🔴 그리고 dev 는 지금 배포가 안 된다

`vercel.json` 에 크론이 4개인데 **Vercel Hobby 는 개수와 빈도가 둘 다 묶여 있다.** 전에 매시간 크론을 걸었다가 배포가 통째로 거부된 적이 있다(`b91785e`).

> 📌 **빌드 자체는 멀쩡하다** (2026-09-16 실측: `origin/dev` 99393a1 → `✓ Compiled successfully`,
> 정적 페이지 88개). 막히는 건 Vercel 의 크론 제한이지 코드가 아니다. 이 PR 이 4→1 로
> 합치면서 그 제한을 푼다 — **그래서 이게 스택 맨 아래고, 여기가 막히면 나머지 8개가 멈춘다.**

### 충돌 해소 — 전부 "dev 쪽을 살린다"

- **`booking-sweep`** — 되살렸다. 지운 쪽을 택하면 방금 들어온 **전달 기한 초과 알림이 통째로 죽는다.** 취소·환불 정책 제10조 2항(14일 초과 시 전액 환불)과 직결된다. `markPastShootsAsShot` 이 할 일 없다는 판단은 여전히 맞지만 그게 알림을 죽일 이유는 못 된다 — 한 크론 안에 둘이 같이 있을 뿐이다
- **`auth/callback`** — 양쪽 다. main 의 `kakao=nophone` 분기 + dev 의 약관 동의 게이트. 둘 다 `dest` 를 건드려서 `const` → `let`
- **`chat/page`** — import 두 줄
- `daily-digest` · `expire-inquiries` 는 충돌 없이 삭제 반영 (dev 가 그 뒤로 손댄 적이 없다)

### 크론 4 → 1

```
vercel.json:  /api/cron/daily  ·  0 0 * * *  (09:00 KST)
```

그 하나가 네 작업을 급한 것부터 순서대로 부른다.

1. `refund-notices` — 환불 구간 전환 예고. 구간이 닫히기 전에 나가야 하고, 안 나가면 그게 곧 분쟁이다
2. `delivery-overdue` — 결과물 전달 기한 초과 (정책 5단계)
3. `past-shoots` — 촬영 시각 지난 예약 → '촬영 완료'
4. `chat-reminders` — 미열람 채팅 리마인더

**⚠️ 한 작업이 실패해도 나머지는 돈다.** 크론이 따로였을 땐 요청이 각자라 공짜로 딸려오던 성질인데, 합치면 잃기 쉽다. `run()` 으로 작업마다 감싸서 throw 든 `ok:false` 든 그 안에 가둔다. 하나라도 실패하면 500 을 돌려주되 **남은 작업을 전부 시도한 뒤**다. 응답 본문에 작업별 결과가 그대로 들어간다.

**개별 엔드포인트는 전부 남겼다.** 손으로 하나만 돌려보거나, Supabase pg_cron + pg_net 으로 더 자주 때릴 때를 위해서다 — 특히 `chat-reminder` 는 주기가 아니라 스캐너라 자주 부를수록 정밀해진다(12~36시간 → 12~13시간). `vercel.json` 에 거는 것만 하나로 줄였을 뿐 엔드포인트를 없앤 게 아니다.

➕ `cron-auth.ts` — `CRON_SECRET` Bearer 검증을 크론마다 여섯 줄씩 복붙하고 있었다. 한 군데서 틀리면 그 엔드포인트만 조용히 열린 채 남는다.

### 확인할 것

- [ ] 배포 후 `/api/cron/daily` 가 크론 목록에 하나로 잡히는지
- [ ] 첫 회차 응답 본문의 `tasks[]` 네 개가 전부 `ok: true` 인지
- [ ] 전달 기한 초과 알림이 실제로 나가는지 (5단계 작성자 확인 필요)

---

## PR 2 — `qa/ui-ux-0911-stack` → `chore/sync-dev-main`

9/11 공개 지면 QA 후속 **51커밋.** 정책 코드와는 다른 층이라 충돌은 다섯 파일뿐이었다.

### 내용

- **대비** — 공개 지면 위반 0. 브랜드 채움색을 `--brand-solid` 로, 틴트 위 채움색을 `-ink` 로, 반투명 글자색(`text-fg/NN`) 158곳을 역할 토큰으로
- **터치 타겟** — 캐러셀 도트 6×6 → 32×44, 작가 내비 32~36 → 44px
- **제목 구조** — h1 누락·중복·건너뜀 해소
- **푸터** — 무한 피드를 한 번 끊어 되찾았다. 모바일엔 사업자 정보가 아예 없었다. 읽는 지면·약관에도 붙였다
- **반응형** — 모바일 가로 넘침 두 곳(작가 가용시간·어드민 계산기)
- 배너 무한 루프 · 등장 연출 빈칸 · 리사이즈 trim · 카카오 프로필 사진 백필

### 충돌 해소 — 전부 "dev 의 내용 + 내 토큰"

| 파일 | 해소 |
|---|---|
| `AdminBookings` | 둘 다 dev. 환불 표기는 dev 가 정책(위약금 4단)을 새로 그린 것이고, '정산 완료 마킹' 버튼은 dev 가 warning → 중립(`text-fg`)으로 의도적으로 낮췄다. 내 수정은 `text-warning` → `-ink` 대비 하나뿐이라 dev 판에 얹을 게 없다 |
| `settlements` | dev 의 문구("중개 수수료(20%)와 부가세") + 내 토큰 |
| `settings` | dev 가 새로 넣은 '알림 받을 연락처' 구역 유지 + 내 토큰 둘 |
| `terms` | dev 의 '함께 적용되는 문서' 구역 유지 + 내 `SiteFooter` 를 그 뒤에 |
| `auth/callback` | 양쪽 다. 번호는 `provider_token` 까지 넘기는 dev 판, 카카오 프로필 사진 백필은 내 것 |

### ➕ 정책 4단계가 다시 들여온 반투명 글자색 7곳

`673cb5b` sweep 이후 올라간 코드라 그때 못 훑은 자리다. 전부 작가 입점 동의 게이트·사업자 정보 — **세금과 계약이 걸린 문구들**이다.

```
AgreeGate    legend ×3 (text-fg/55 4.17:1) · 필드 힌트 ×2 (text-fg/45 3.04:1)
ProfileForm  legend ×1 · 사업자 유형 힌트 ×1
```

숫자 매핑이 아니라 **역할**을 봤다. "일반과세자는 실질 20%, 미등록은 3.3% 원천징수" 는 플레이스홀더가 아니라 보조 텍스트다. legend → `text-muted`(이 repo 의 다른 legend 셋이 쓰는 토큰), 힌트 → `text-faint`(`ProfileForm` 의 `Field` 가 자기 hint 에 쓰는 토큰).

### ⚠️ 남겨둔 것 — 14곳

dev 가 들여온 게 아니라 **9/11 sweep 이 지나친 자리**다. 대부분 아이콘 버튼과 칩의 비활성 상태(studio·admin)라 본문 글자와 판단 기준이 다르다. 따로 재야 하고 이 PR 에 섞을 일이 아니다.

```
studio/booking/BookingFieldsEditor ×4 · portfolio ×4 · guide ×1 · packages ×1
settlements ×1 · admin/explore ×1 · apply/ApplyLeadForm ×1 · ui/EmptyState ×1
```

---

## PR 3 — `feat/home-first-screen` → `qa/ui-ux-0911-stack`

**모바일에서 첫 사진이 800px 아래에 있었다.** 스크롤 한 번을 해야 사진이 보이는 사진 서비스였다. → **578px.**

- **상단 한 줄** — 로고 ─ 검색 ─ 프로필 병합. 로고는 요소 박스가 아니라 **잉크**를 기준으로 맞췄다(`translate-y-[-0.125em]`) — 박스만 맞추면 3px 낮아 보인다
- **검색창** — 원래 자리를 지나면 떠오른다. 폭이 안 변하면 작은 검색창이 중앙에 댕그러니 남아서, `left`/`width` 를 2단계 rAF 로 보간한다(260ms)
- **배너** — 도트를 사진 안 우하단으로, 모바일 비율 2:1
- **무드** — 라벨을 사진 안으로, `[모두 보기]` 를 제목 줄로 흡수
- **바로가기** — 다섯을 실루엣으로 가른다. 틴트 원 제거. 첫 칸은 '아티클'
- **`/c/[slug]`** — 홈과 같은 지면으로. 검색·바로가기·무드가 통째로 없었다. 광고 진입 판정은 `sp.ad` (기존 `adAsGallery` 는 일반 방문에도 non-null 이라 오작동)
- **새로고침 스크롤** — 스켈레톤(1806px)이 복원값 1500 을 962 로 클램프하고 **그 클램프값이 저장되던** 것. `scrollRestoration='manual'` 을 `<head>` 인라인으로 + 네비게이션 타입 판정
- **무한 피드** — 자동 연장을 3회 → 1회. 모바일에서 푸터까지 17화면이었다
- **맨 위로 버튼** — 구석으로, 바텀바와 세로 중심 정렬

### ⚠️ 바로가기 아이콘은 세 번 반려됐다

채움 아이콘 → 칩별 틴트 → 컬러 일러스트 전부 "디자인 시스템과 안 어울린다". 원인은 내가 **레퍼런스 스크린샷만 보고 `docs/14-design-system.md` 를 안 읽은 것**이다. 토큰만 쓰는 선 아이콘으로 되돌렸고, 규칙 두 개를 주석으로 남겼다 — *바깥 실루엣이 서로 달라야 한다* · *광학 중심을 뷰박스 중심(12,12)에 맞춘다*.

### 확인할 것

- [ ] 실기기에서 첫 화면에 사진이 보이는지
- [ ] 검색창 떠오를 때 폭 전환이 어색하지 않은지
- [ ] 새로고침했을 때 스켈레톤·본문 모두 최상단에서 시작하는지

---

## PR 4 — `feat/magazine-deck` → `feat/home-first-screen`

- **아티클 덱** — 무한 루프(`[...a,...a,...a]`), 카드 높이 축소(모바일 1:1). 첫 아티클이 중앙일 때 왼쪽에 마지막 아티클이 보인다
- **SPOTS** — 끝에서 당기면 전체보기로. 당겨 새로고침의 가로판. 터치·트랙패드 둘 다
- **섹션 등장 연출** + 상단·섹션 여백 조정

### 밟은 함정 둘

**덱이 3↔4 에서 드드드득 떨었다.** `wrap()` 이 관성 중에 돌면서 `scroll-snap-type: x mandatory` 와 싸운 것. **`scrollend` 에서만** 감는다(타이머 폴백 140ms).

> 첫 진단은 틀렸다. `scrollLeft += 60` 으로 재려 했는데 스냅이 되돌려서 1487 → 1487 이 나왔다. 프로그래밍 스크롤은 스냅 버그를 못 잡는다 — 터치를 합성해서 다시 쟀다.

**SPOTS 가 160px 끌어도 15px 만 열렸다.** 인디케이터가 스크롤러 **안에** 있어서, 열리는 만큼 `scrollWidth` 가 늘어 스크롤이 안 끝난 것. 인디케이터를 밖으로 빼고 레일을 `transform` 으로 민다.

### 확인할 것

- [ ] 실기기에서 덱 빠르게 스와이프
- [ ] SPOTS 당기기 (터치)

---

## PR 5 — `feat/guide-spots-db` → `feat/magazine-deck`

Q&A(`guide_items`) · 촬영 장소(`spots`) 를 하드코딩 파일에서 **DB + 어드민 CRUD** 로.

- `supabase/migrations/0116_guide_spots.sql` — 두 테이블 · RLS · 트리거 · **55행 시드**. 멱등
- `/admin/guide` · `/admin/spots` — 한 지면에서 편집
- 소비처 7곳 전부 재배선: `sitemap` · `/guide` · `/guide/[slug]` · `/explore` · `/spots` · `/spots/[slug]` · `lib/spots.ts`

### 설계 메모

- `spots.descr` — `desc` 는 SQL 예약어
- `axis` 는 enum 이 아니라 `text + check` — 운영 중 값 추가에 마이그레이션이 필요 없다
- `listPublishedGuideItems()` 는 `createPublicClient` — `cookies()` 를 쓰면 `generateStaticParams` 가 빌드에서 터진다 (`spots.ts` 에 같은 이유가 이미 적혀 있었다)
- `toggleSpotPublished` 는 **`source` 없이 발행을 거부한다** — 출처 없는 장소 정보를 공개 지면에 올리지 않는다

### ⚠️ 마이그레이션 번호

`0112` → `0116` 으로 옮겼다. dev 의 `0112_consent_records.sql` 과 충돌했다.

**그런데 dev·main 분기 때문에 번호가 이미 겹쳐 있다** — 이 PR 이 만든 게 아니고, 고치지도 않았다(적용된 마이그레이션 이름을 바꾸면 Supabase 가 새 것으로 본다). 알고는 있어야 한다:

```
0111_photographer_read_at.sql        (main)
0111_refund_policy_v1.sql            (dev)
0112_consent_records.sql             (dev)
0112_notify_kind_booking_proposed_split.sql  (main)
```

### 남은 정리

`src/lib/guide-data.ts`(317줄) · `src/lib/spots-data.ts`(366줄) 는 이제 아무도 안 쓴다. **어드민 왕복 확인 뒤** 지운다.

### 확인할 것

- [ ] 마이그레이션 적용 후 `/guide` · `/spots` 가 DB 값으로 뜨는지
- [ ] `/admin/spots` 키워드 배열 왕복 (curl 로는 확인 불가)
- [ ] `/admin/guide` 줄바꿈 보존

---

## PR 6 — `feat/cart-expand-anim` → `feat/guide-spots-db`

장바구니 펼침 애니메이션 복구 — **도크 → 중앙 → 작가 줄 자리로.** 팀원이 장바구니 내부 UI 를 작가 단위로 바꾸면서 사라졌던 것.

날아가는 건 **카드 레이어 자체**다. 중앙 더미에서 펼쳐져 최종 자리로 간 뒤 실제 줄과 교대한다.

### 재정렬이 보이던 것 — 원인 셋이 겹쳐 있었다

착지 직후 카드가 잠깐 멈췄다 다시 정렬됐다. 정사각 0도로 깔렸다가 회전이 붙는 모양.

1. 착지 각도가 최종 각도와 달랐다
2. 카드 박스 규격이 두 군데서 따로 계산됐다
3. **줄 카드 자신의 등장 애니메이션(`fill: both`)이 측정을 정확히 14px 밀고 있었다**

`cartRowCardRotation()` · `cartRowCardBox()` 를 단일 출처로 빼고, 줄 카드의 등장 keyframes 를 지웠다. 이제 dx 0 · dy 0 · w 0 · h 0 · 각도 일치로 교대한다.

### 확인할 것

- [ ] 실기기에서 장바구니 탭 → 펼침에 멈칫이 없는지

---

## PR 7 — `feat/photographer-onboarding` → `feat/cart-expand-anim`

작가가 처음 와서 스튜디오 문이 열리기까지, 그 한 줄을 다시 깔았다.

### 진입로가 세 단계였다

뿌릴 주소가 없었다. `/apply` 는 로그인 게이트라 처음 온 사람은 로그인 화면만 봤고, 신청
버튼은 가입 뒤 프로필 메뉴를 뒤져야 나왔다. **링크 하나로 끝날 일이 세 단계였다.**

이제 `/apply` 가 모집 주소다. 비로그인은 안내 지면을 보고, 가입/로그인 뒤 그 자리로 돌아와
바로 폼을 만난다. 프로필 탭의 [작가 신청]은 뺐다 — 진입로가 둘이면 둘 다 관리해야 한다.

### 약관을 읽을 기회를 실제로 준다

전에는 체크박스 넷과 [모두 동의] 였다. 약관규제법 3조에서 사업자가 대는 근거가 "읽을 기회를
줬다" 인데, **[모두 동의] 는 그 기회를 없애는 버튼이다.**

전문을 지면에 깔고 끝까지 스크롤해야 동의가 열린다. 문서별로 연 시각과 동의 시각을 따로
남긴다(`doc_records`, 마이그레이션 0117). 본문은 공용 컴포넌트로 빼서 **약관 지면과 동의
플로우가 같은 글을 본다** — 따로 두면 한쪽만 개정된다.

### 카카오가 이미 받은 동의를 두 번 받지 않는다

간편가입을 켜고(`docs/34-kakao-sync-handoff.md`), 카카오 동의 화면에서 받은 약관 동의를
`/auth/callback` 이 우리 기록으로 옮긴다. 그래서 정규 흐름에서 약관·연락처 화면이 사라졌다
— 다만 코드는 남겼다. 전화번호(선택 동의)를 거부했거나 조회가 실패하면 그때 뜨는 폴백이다.

### QA 하려고 만든 것 둘

- `/dev/test-login` — 카카오 없이 `@samae.test` 계정으로 들어간다. 가드 세 겹
- `/dev/flow` — 온보딩 샌드박스. **지면은 전부 실제 컴포넌트**고 저장만 localStorage 로
  돌린다. 처음엔 화면을 손으로 흉내 냈다가 "실제와 다른 걸 QA 하게 된다" 로 갈아엎었다.
  렌더 DOM 을 실제 지면과 대조해 23/23 일치를 확인했다

`scripts/qa-photographer.cjs` 로 계정을 아무 단계로나 되감을 수 있다.

### 확인할 것

- [ ] 로그아웃 상태 `/apply` 가 랜딩을 그리는가 (예전엔 로그인으로 튕겼다)
- [ ] 가입 → **다시 `/apply` 폼**으로 돌아오는가
- [ ] 약관 4종을 끝까지 내려야 동의가 열리는가 · 문서를 바꿔도 잠금이 유지되는가
- [ ] 프로필 탭에 [작가 신청]이 **없는가**
- [ ] 프로덕션에서 `/dev/*` 가 404 인가 (`proxy.ts`)

---

## PR 8 — `feat/tax-and-payment-gate` → `feat/photographer-onboarding`

돈이 갈라지는 자리 셋. **커밋 3개지만 제일 조심해서 볼 PR이다.**

### 주민등록번호 (마이그레이션 0118)

고유식별정보다. 개인정보보호법 24조의2 상 **법령 근거가 있을 때만** 처리할 수 있고,
우리 근거는 소득세법 127·164조(원천징수·지급명세서)뿐이라 쓰임도 그것뿐이어야 한다.

```
수집   사업자 미등록일 때만 (일반·간이는 세금계산서 → 대상 아님, 근거 없는 수집이 된다)
검증   13자리 체크섬 + 생년월일 정합성 (13월 45일이 체크섬만으론 통과한다)
저장   AES-256-GCM. 평문 컬럼 없음. 키 없으면 던진다
표시   화면·목록·로그 전부 마스킹 (900101-1******)
파기   사업자 유형 바꾸면 세 컬럼 즉시 null (법 21조)
열람   통로 하나뿐. 로그를 먼저 남기고 그다음에 연다
```

**키가 없으면 던진다.** 평문으로 떨어지게 두면 키를 안 넣은 환경에서 조용히 평문이 쌓이고,
나중에 되돌릴 방법이 없다. **로그를 먼저 남기고 연다** — 열고 나서 남기면 로그 실패 시
"기록 없는 열람" 이 성립한다. 테스트 11개.

### 원천징수 3.3% (마이그레이션 0119)

09-10 에 "중개자라 원천징수 안 함" 으로 정했던 걸 뒤집는다. 소득세법 127조의 원천징수의무자는
**지급하는 자**이고, 고객 돈이 사매 계좌를 거쳐 나가므로 지급자는 사매다. 매출을 수수료만
잡는 것과 충돌하지 않는다 — 매출 인식은 '누구의 거래냐', 원천징수는 '누가 돈을 건네냐'다.

소액부징수(소득세 1,000원 미만)와 10원 미만 절사까지 넣었다. 테스트 11개.

> ⚠️ **과세표준을 예약 대금 전체로 잡았다.** 수수료 뺀 실지급액이 아니다. 30만원 건에서
> 9,900원 vs 7,722원 차이다. **세무사 확인이 남았다** — 기준이 다르면 `withholding.ts` 한 줄.

작업 중에 어드민 「작가 송금액」이 **부가세를 안 빼고** 있던 걸 찾았다(`amount − feeKrw`).
운영자가 그 숫자를 은행 앱에 옮겨 적으므로 그만큼 과다 송금된다. 같이 맞췄다.

### 예약 상세의 결제 게이트가 통째로 비어 있었다

같은 결제를 두 자리에서 하는데 한쪽만 막혀 있었다.

```
/chat           AcceptPayDialog → 임박 동의 + 위약금 표 + 확인 체크 → 입금완료
/bookings/[id]                                                      입금완료
```

알림 링크가 전부 `/bookings/[id]` 라 오히려 이쪽이 자주 지나가는 길이다. 여기서 결제하면
`late_booking_consent_at` 이 비고, 결제 7일 안에 취소하면 `refundQuote` 가 **전액 환불**로
판정한다. 촬영 3일 전 30만원 예약이면 **위약금 27만원이 통째로 사라진다.**

`DepositGate` 로 묶어 양쪽이 같은 게이트를 쓴다. **임박 예약은 동의 전까지 계좌를 가린다** —
번호가 먼저 보이면 동의 화면을 거치지 않고 송금해 버리고, 그러면 고지가 성립하지 않는다.

### 확인할 것

- [ ] `RESIDENT_NO_KEY` 가 Production·Preview 에 있는가 (없으면 미등록 작가 동의가 막힌다 — 일부러 그렇다)
- [ ] 일반·간이과세자에게 주민번호 입력란이 **안 뜨는가**
- [ ] 사업자 유형을 바꾸면 저장된 값이 지워지는가
- [ ] 미등록 작가 정산 화면에 원천징수 줄과 안내가 뜨는가 · 사업자 작가에겐 안 뜨는가
- [ ] 어드민 송금액 = 대금 − 수수료 − 부가세 − 원천징수
- [ ] 촬영 7일 안쪽 예약을 `/bookings/[id]` 로 열면 계좌가 가려지는가

---

## PR 9 — `qa/photographer-flow-harness` → `feat/tax-and-payment-gate`

환불 운영 절차 · 연락처 게이트 · 지급명세서 · 정산 기한, 그리고 **그 전부를 세 입장에서
눌러 볼 수 있는 QA 무대.** 스택에서 가장 크지만 절반이 QA 도구라 운영 코드 면적은 작다.

### 환불은 계산만 맞고 절차가 없었다 (마이그레이션 0120)

신청이 들어와도 아무도 모르고, 어드민이 버튼을 누르면 작가와 한마디 없이 즉시 실행되고,
실행하면 돈이 나간 것처럼 닫혔다. 네 단계로 나눴다.

```
① 신청   고객 [사매에 문의 → 환불]  →  디스코드 알림       ← 없었다
② 합의   작가와 이야기 → 접수함에서 [작가 합의 확인]       ← 없었다
③ 실행   거래 화면 [환불] — ②가 없으면 잠긴다              ← 즉시 실행이었다
④ 송금   PG 지급대행에서 보내고 [송금 완료로 기록]          ← 없었다
```

**①이 제일 위험했다.** 신청 시각이 곧 취소 시점(취소환불 5조 3항)이고 거기서 **3영업일**
안에 환급해야 하는데(전자상거래법 18조 2항, 초과 시 연 15% 지연이자), `support_requests` 에
행만 쌓이고 아무도 안 봤다. **알림이 없다는 것 자체가 기한 초과의 원인이었다.**

②는 버튼만 잠그면 폼 위조로 통과하므로 `adminRefund` 서버에서도 막는다. 운영 판정
(작가 귀책·천재지변·노쇼)에는 안 건다 — 그건 작가와 합의할 성질이 아니다.

④ `refundBooking()` 은 원장 정리일 뿐 돈을 옮기지 않는다. 나눠 남기지 않으면 "환불됨" 으로
닫힌 건이 실은 입금이 안 된 상태로 남고, 고객이 항의하기 전엔 아무도 모른다.

**퍼센트는 신청일 기준이 맞다.** 합의 절차가 들어오면서 접수와 실행 사이가 며칠씩 벌어지므로
그 경계를 테스트로 못박았다 — 신청 9/13(40%)을 9/18 에 실행해도 40%, 촬영이 지난 뒤에 눌러도
`after_shoot` 로 안 떨어진다.

### 샌드박스 — 지면은 전부 실제 컴포넌트다

```
/dev/chat    ① 임박 제안 ② 여유 제안 ③ 수락 후 재입장 ④ 입금완료 후 ⑤ 입금확인(r 로 역할 전환)
/dev/pay     예약 상세의 결제 구간 4단계
/dev/money   정산 금액 — 사업자 유형 토글
```

마크업을 베끼지 않으려고 `DepositGate` · `SettlementsBody` · `BookingMoney` · `ChatShell` 을
떼어내 **실제 지면과 샌드박스가 같은 파일을 쓴다.** 방이 바깥으로 나가는 통로는 `chat-io.ts`
한 곳에 모아 샌드박스가 그 객체만 갈아 끼운다 — 기본값이 진짜 함수들이라 **실제 방의 동작은
한 글자도 달라지지 않는다.**

⚠️ **스텁이 no-op 이면 안 된다.** 실제 컴포넌트들은 액션 뒤에 `router.refresh()` 를 부르는데
서버가 같은 고정값을 다시 내려주면 화면이 한 발짝도 안 나간다. 그래서 스텁이 예약 스냅샷을
고치고 그 변화로 방을 다시 그린다.

`/dev/refund-test` 는 **진짜 경로로** 알림을 한 번 쏴 본다. 문구를 손으로 만들어 웹훅에 던지면
정작 진짜 경로는 확인이 안 된 채 남는다. 서버 액션의 핵심을 `lib/support-requests.ts` 로
떼어내 액션과 방아쇠가 같은 함수를 부른다. `?undo=<요청id>` 로 흔적을 치운다.

### 🔴 연락처 전달을 촬영 7일 전부터로 막는다 (HANDOFF §3-1)

**이건 한 번 놓쳤다가 뒤늦게 넣은 것이다.** `docs/32 §3-3` 이 폐기한 *"시간이 지나면
저절로 열린다"*(고지·동의 없이 자동 공개)와 같은 것으로 잘못 읽어 건너뛰었다. 방향이
반대다 — 여기서는 두 단계(작가가 보내고 → 고객이 고지 읽고 받는다)를 **그대로 두고
시작점만 미룬다.**

왜 필요한가. 신 정책은 촬영 8일 이상 전 취소의 위약금이 0% 다. 입금 확인 직후부터
연락처가 나가면 둘이 겹쳐 이탈 경로가 열린다:

```
① 촬영 30일 전 결제 → ② 작가가 연락처 전달 → ③ "취소하고 직접 하시죠"
→ ④ 8일 이상 전이라 위약금 0%, 전액 환불 → ⑤ 직거래
고객 이득 · 작가 이득 · 사매는 PG 수수료만 날린다(환불해도 반환되지 않는다)
```

구 정책은 *연락처를 받으면 100% 구간이 닫힌다* 로 막았는데, 신 정책에서 그 조항이 빠지며
방어가 사라졌다. 그래서 **전달 가능 시점을 위약금이 붙는 시점과 맞춘다.** 7일 전부터면
취소 시 위약금 40% 라 고객이 실제로 돈을 날려야 하고, 작가가 그 이상 깎아주면 작가도
남는 게 없어 유인이 스스로 사라진다.

`lib/contact-gate.ts`(테스트 11개) · 서버 가드 · 버튼 자리의 «촬영 7일 전부터» 안내.
버튼만 감추면 폼 위조로 들어오므로 서버가 막는다.

⚠️ **촬영일이 없는 예약도 닫는다.** 남은 날수를 셀 수 없어 취소가 늘 전액 환불이라
   이탈 유인이 가장 큰 자리다. 인계 문서에 없던 판단이라 다르게 보면 한 줄이면 바뀐다.

### 지급명세서 — 뗀 세금을 신고까지 끌고 간다

원천징수는 떼는 것으로 끝나지 않는다. **신고·납부해야 끝난다.** 안 하면 작가는 낸 적
없는 세금으로 기록되고 우리는 미납이 된다. 복호화 통로와 정산 스냅샷만 있고 서류를 만드는
자리가 없었다.

```
/admin/withholding                월별 지급 인원·건수·소득세·지방소득세 + 신고 기한
/admin/withholding/export?period= 지급명세서 CSV
```

지급일은 **정산 송금 시각(`settled_at`)** 이다. 금액은 정산 때 굳힌 `settlement_breakdown`
에서 읽는다 — 다시 계산하면 요율·사업자 유형이 바뀌었을 때 **이미 통장에 들어간 금액과
다른 숫자**를 신고하게 된다.

주민등록번호는 화면에 평문으로 안 띄운다. CSV 내보내기가 서버에서 `readResidentNo()` 로
열고 **로그를 먼저 남긴다.** 라우트에서 어드민을 다시 확인한다 — 라우트 핸들러는 레이아웃
가드를 안 탄다(403 실측). 집계·CSV 는 순수 함수로 빼 테스트 15개.

### 정산 7영업일 기한 표시

정책은 전달 알림으로부터 7영업일 안에 지급하라고 한다(수수료·정산 정책 2조). 자동 지급
전까지는 어드민이 직접 누르는데(송금이 KG 상점관리자에서 수동이라 아직 자동화 불가),
**목록에 "언제까지" 가 없었다.** 환불은 3영업일 초과를 이미 강조하는데 정산만 없었다.

접힌 줄의 배지 · 급한 순 정렬 · 상세의 기산점. 영업일 셈법은 `lib/business-days.ts` 로
모아 환불(3영업일)과 함께 쓴다.

### QA 무대 — 세 입장에서 직접 돌린다

어드민 큐는 **해당 상태의 예약이 실제로 있어야** 보인다. 손으로 만들면 한 바퀴에 반나절이
걸리고 결국 안 돌게 된다.

```
node scripts/qa-seed.cjs seed | list | clean     모든 상태의 예약 10건
/dev                                             역할별 허브 (샌드박스 / 실선 구분)
```

무대는 고객 `roleplay-customer` × 작가 **김재즈**(= 정훈 본인 계정) 사이에 선다 —
어드민·작가는 한 로그인으로 보고 고객만 시크릿 창으로 가르면 된다.

⚠️ 운영 DB 에 진짜 행이 생긴다(별도 dev 프로젝트가 없다). 메모의 `[QA무대]` 표식만
`clean` 이 지운다.

### 곁들여 잡은 것

- `lib/booking-format.ts` 신설 — `lib/bookings` 가 `server-only` 라, 클라이언트가 포매터
  하나만 가져와도 `next/headers` 까지 번들에 딸려온다(`/dev/money` 가 실제로 그렇게 터졌다)
- `FloatingNav` 가 `/dev/chat` 에서 안 빠져 채팅 샌드박스에 하단 내비가 떴다
- 어드민 환불 미리보기가 `feeRate` 를 안 넘겨 위약금 배분이 20% 로 고정돼 있었다

### 확인할 것

- [ ] 환불 신청 → 디스코드 알림이 오는가 (실측 완료 · 입금 전/후 두 건)
- [ ] 작가 합의 전에 [환불] 이 잠기는가 · 폼을 위조해도 서버가 막는가
- [ ] 환불 실행 후 PG 송금 블록이 뜨는가 · [송금 완료로 기록] 이 멱등인가
- [ ] 프로덕션에서 `/dev/*` 가 전부 404 인가
- [ ] 촬영 30일 뒤 건에서 [연락처 보내기] 대신 «촬영 7일 전부터» 안내가 뜨는가
- [ ] 정산 대기 큐가 **급한 순**으로 정렬되고 초과 건에 배지가 붙는가
- [ ] `/admin/withholding` CSV 를 받으면 `resident_no_access_logs` 에 열람 기록이 남는가
- [ ] 프로덕션에서 `/dev/*` · `/dev/refund-test` 가 전부 404 인가
- [ ] `NEXT_PUBLIC_PG_PAYOUT_URL` — **KG 상점관리자 주소.** 9/22 개통이라 그때 채운다.
      비면 바로가기만 안 뜨고 [송금 완료로 기록]은 그대로 동작한다
