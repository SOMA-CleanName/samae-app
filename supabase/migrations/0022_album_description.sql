-- ─────────────────────────────────────────────
-- 0022. 피드(앨범) 설명글
--
-- 작가가 촬영(피드)에 대한 설명을 덧붙일 수 있게 한다. 사진 상세에서 노출.
-- 피드 단위라 albums 에 둔다(같이 올린 사진들이 한 설명을 공유).
-- ─────────────────────────────────────────────
alter table public.albums
  add column if not exists description text;
