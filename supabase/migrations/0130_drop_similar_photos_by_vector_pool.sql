-- ════════════════════════════════════════════════════════════════
-- 0130 · similar_photos_by_vector 의 pool 판(vector, int, int)을 지운다 — 0127 뒤에서 한 번 더
--
-- 이 이름의 함수가 둘이면 앱 호출이 통째로 실패한다(docs/22 §6.4).
--
--   (halfvec, integer)            0079 → 0128   앱이 부르는 것. 남긴다
--   (vector, integer, integer)    0127          pool 판. 지운다
--
-- 앱은 p_embedding(JSON 문자열)·p_limit 두 값만 넘긴다. 문자열은 halfvec 로도 vector 로도 맞고
-- pool 판의 p_pool 에 기본값이 있어 두 값으로도 불리므로, 둘이 있으면 Postgres 가 고르지
-- 못한다("function ... is not unique"). 9/17 오후 약 한 시간 동안 SigLIP 검색·페르소나 추천이 그랬다.
--
-- pool 판은 쓰는 데가 없다 (2026-09-18 확인).
--   · 모든 브랜치·닫힌 PR 포함 커밋 1,426개에서 p_pool 을 넘겨 부르는 코드는 0곳.
--     팀원의 페르소나 코드(8/20)도 두 값만 넘긴다 — 쓰는 건 halfvec 판이다.
--   · PR #355 본문도 "앱은 인자 두 개만 넘겨 halfvec 쪽이 뽑힌다" 고 적었다.
--   · 운영 DB 안에서 이 함수를 부르는 함수·예약 작업·뷰 0개.
--   · 0127 의 목적(숨긴 사진 거르기)은 0128 이 halfvec 판에 넣었고 운영에서 동작 중이다.
--   · pool 판은 search_path 가 public 뿐이라 거리 연산자(<=>)를 못 찾아 부를 때마다 42883
--     으로 실패했다 — 있던 동안에도 동작한 적이 없다.
--
-- 운영 DB 에는 이미 없다(0121 이 2026-09-17 17:49 에 지웠다). 여기서는 아무 일도 안 일어난다.
-- 이 파일이 필요한 건 **번호 순서대로 다시 돌리는 DB**(빈 DB·스테이징) 때문이다 —
-- 0121(지움, 그때는 아직 없음) → 0127(생성) 순서라 끝 상태가 둘이 된다. 0127 뒤에서 한 번 더 지운다.
--
-- ⚠️ 0079·0127·0128 을 손으로 다시 돌리지 말 것. 셋 다 `create or replace` 라서, 시그니처가
--    다른 함수를 **하나 더** 만들어 다시 둘이 된다. 겹침은 `node scripts/check-rpc-overloads.cjs`.
--    나중에 후보 수 조절(p_pool)이 필요해지면 halfvec 판에 선택 인자로 넣는다 — 이름이 같은
--    함수를 새로 만들지 말고 drop 하고 다시 만든다.
-- ════════════════════════════════════════════════════════════════

drop function if exists public.similar_photos_by_vector(extensions.vector, integer, integer);
