-- ════════════════════════════════════════════════════════════════
-- 0025 · 출장비를 자유 텍스트로
--   · 고정 금액(travel_fee_krw) 대신 작가가 자유롭게 적는 안내 텍스트를 추가.
--   · 예약 설정에서 텍스트로 저장하고, 예약 제안 화면에 안내로 노출한다.
--     (기존 travel_fee_krw 컬럼은 호환을 위해 남기되 신규 입력은 0으로 둔다)
-- ════════════════════════════════════════════════════════════════

alter table public.photographers
  add column if not exists travel_fee_note text;
