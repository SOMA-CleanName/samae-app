-- ─────────────────────────────────────────────
-- 0048. 카테고리 광고 소재 사진 채택
--
-- 카테고리별 광고에 사용할 "채택 사진"을 운영자가 어드민에서 고른다.
-- 광고 URL(/?ad=<사진ID>&cat=<slug>)을 클릭하면 그 사진이 메인 좌상단 첫
-- 카드(스포트라이트 튜토리얼)로 뜨고, 동시에 해당 카테고리 알고리즘 피드로 진입한다.
-- A/B 소재 운용을 위해 여러 장을 채택할 수 있으므로 배열로 둔다.
-- (사진 삭제 시 정합성은 읽는 쪽에서 검증 — 배열 FK 는 PG 미지원)
-- ─────────────────────────────────────────────
alter table public.categories
  add column if not exists ad_photo_ids uuid[] not null default '{}';
